import Card, { Cards } from '@components/core/Card';
import guides from './developing/_meta.en-US.json';

# Developer Guides

With just a few simple API calls, create unique live and on-demand video experiences that play anywhere and look beautiful, every time, at scale.

<Cards
  num={3}
  children={Object.keys(guides).map((key, i) => (
    <Card
      key={i}
      title={guides[key].title}
      description={guides[key].description}
      icon={guides[key].icon}
      href={guides[key].href}
    />
  ))}
/>
