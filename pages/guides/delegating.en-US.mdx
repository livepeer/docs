import Card, { Cards } from '@components/core/Card';
import guides from './delegating/_meta.en-US.json';

# Delegator Guides

These guides will help you get started as a delegator of LPT on the Livepeer network.

<Cards
  num={3}
  items={guides}
  children={Object.keys(guides).map((key, i) => (
    <Card
      key={i}
      title={guides[key].title}
      description={guides[key].description}
      icon={guides[key].icon}
      href={guides[key].href}
    />
  ))}
/>
