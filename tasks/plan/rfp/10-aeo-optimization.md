# Task RFP-M10: AEO optimization

**Missing idea:** M10  
**RFP deliverable:** (iii) V1 Documentation Live

---

## Objective

Add **AEO (Answer Engine Optimization)** to the docs site so that the RFP requirement “SEO and **AEO optimization**” is met alongside existing SEO work.

## Scope

- RFP (iii): “Set up redirects, **SEO and AEO optimization**, and accessibility compliance (WCAG)”
- Report mentions SEO; AEO is not mentioned in task list or report
- AEO = optimizing for AI/answer engines (e.g. structured data, clear Q&A, semantic markup that helps LLMs and search answer features)

## Deliverables

- Research: what AEO means for Mintlify/docs (structured data, schema, summaries, FAQ markup)
- Implementation: apply AEO best practices (e.g. schema.org, clear headings/summaries, any Mintlify AEO features)
- Document: short note in report or Notion that “AEO optimization” is done and what was done

## Acceptance criteria

- [ ] AEO is defined for this project (what we did or what we’re not doing and why)
- [ ] At least one AEO lever is implemented (e.g. structured data, page summaries, or tool-specific AEO)
- [ ] RFP gap M10 is marked complete in the gaps report

---

## Fix and automate (implemented)

### What AEO means for this project

- **Defined:** AEO = metadata and structure that help AI/answer engines (e.g. ChatGPT, Perplexity, Google SGE) and snippet features use our docs: **descriptions**, **keywords**, **semantic headings**, and consistent frontmatter. We do not inject custom JSON-LD (Mintlify handles meta tags); we ensure every page has machine- and human-friendly summaries and structure.

### Implemented levers

1. **Page descriptions (AEO)**  
   - **Script:** `snippets/scripts/generate-seo.js` now adds a **description** when missing, generated from the first paragraph (50–160 chars). This gives every page a clear summary for snippets and LLMs.
   - **Usage:**  
     - Dry run: `node snippets/scripts/generate-seo.js --dry-run`  
     - Apply: `node snippets/scripts/generate-seo.js`  
     - Single file: `node snippets/scripts/generate-seo.js --file=v2/pages/path/to/page.mdx`  
     - From repo root: `npm run generate-seo` (in `v2/`) or run from `snippets/scripts/` with paths above.

2. **AEO quality checks**  
   - **File:** `tests/unit/quality.test.js`  
   - **Checks:**  
     - Missing **description** (warning; run `generate-seo.js` to fix).  
     - **Description length** 50–160 chars (warning if outside range).  
     - **Semantic structure:** presence of an H1 in the first 200 chars (warning if long content with no H1).  
   - **Run:**  
     - All MDX: `node tests/unit/quality.test.js`  
     - Staged only: `node tests/unit/quality.test.js --staged`

3. **Existing SEO (unchanged)**  
   - Keywords and `og:image` / `twitter:image` remain generated by `generate-seo.js`.  
   - Mintlify uses frontmatter for meta tags; no extra schema.org injection added.

### How to automate in CI / pre-deploy

- **Before deploy:** run `node snippets/scripts/generate-seo.js` (or `npm run generate-seo` from `v2/`) so new or updated pages get keywords, description, and images.
- **CI:** run `node tests/unit/quality.test.js` so missing or poor descriptions and missing H1s surface as warnings; fix by running the SEO script or editing frontmatter.

### Checklist for closing M10

- [x] AEO defined for this project (descriptions, keywords, semantic headings; no custom JSON-LD).
- [x] At least one AEO lever implemented (auto-generated descriptions + AEO quality checks).
- [ ] RFP gap M10 marked complete in the gaps report (update `16-rfp-notion-gaps-and-incomplete.md` when closing).

---

## References

- [16-rfp-notion-gaps-and-incomplete.md](../reports/16-rfp-notion-gaps-and-incomplete.md) — M10
- [Forum RFP 3071](https://forum.livepeer.org/t/rfp-documentation-restructure/3071)
- SEO + AEO script: `snippets/scripts/generate-seo.js`
- Quality (SEO/AEO) checks: `tests/unit/quality.test.js`
