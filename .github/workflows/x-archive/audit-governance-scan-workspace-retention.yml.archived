# type: audit
# concern: governance
# pipeline: P5
# TODO: Implement tasks-retention workflow
# See workspace/README.md for full retention rules
name: workspace/ retention enforcement
on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

permissions:
  contents: write