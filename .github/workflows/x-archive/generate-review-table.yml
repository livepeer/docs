name: Generate Review Table

on:
  workflow_dispatch:

jobs:
  generate-review-table:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: |
          echo "Not yet implemented - see generate-review-table.yml in backlog"
