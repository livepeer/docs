name: Build Review Assets

on:
  workflow_dispatch:

jobs:
  build-review-assets:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: |
          echo "Not yet implemented - see build-review-assets.yml in backlog"
