name: Check Docs Index

on:
  pull_request:
    branches:
      - docs-v2
  push:
    branches:
      - docs-v2
  workflow_dispatch:

jobs:
  docs-index-check:
    uses: ./.github/workflows/docs-catalog-governance.yml
    with:
      mode: check-docs-index
    secrets: inherit
