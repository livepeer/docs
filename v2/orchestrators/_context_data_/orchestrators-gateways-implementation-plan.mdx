---
title: "Orchestrators & Gateways Docs — Implementation Plan"
description: "Complete phased guide for implementing ecosystem builder feedback across v2/orchestrators and v2/gateways sections"
sidebarTitle: "Implementation Plan"
keywords: ["livepeer", "orchestrators", "gateways", "go-livepeer", "documentation", "implementation"]
---

import { Note, Warning, Tip, Info } from 'mintlify/components'

> **Context:** This plan implements feedback from eliteproxy (ecosystem builder / core contributor) 
> covering the `v2/orchestrators` and `v2/gateways` sections. Docker compose and installation 
> assets have already been provided and are in progress. This document focuses on what remains, 
> how to structure it, and editorial decisions to make along the way.

---

## Summary of Feedback

The feedback identified four interconnected problems:

| Problem | Section Affected | Impact |
|---|---|---|
| No real deployment path — architecture without operations | `v2/orchestrators` | Orchestrators can't self-serve |
| Studio abstraction dominates; go-livepeer is invisible | Both | Network value not communicated to builders |
| Transcoding / Realtime AI / Batch AI are not differentiated | Both | Wrong prerequisites, wrong config |
| Factual error: gateways don't need NVIDIA drivers | `v2/gateways/run-a-gateway/install/` | Trust issue |

Everything below derives from those four root problems.

---

## Phase 1 — Immediate Fixes (No SME dependency, ship now)

These require no new content from external contributors. They are corrections and structural stubs.

### 1.1 Fix the NVIDIA Driver Factual Error

**File:** `v2/gateways/run-a-gateway/install/docker-install.mdx`  
**Also check:** Any install prerequisite pages under `v2/gateways/run-a-gateway/install/`

**What to do:**  
Search the entire gateways section for any mention of NVIDIA drivers as a gateway prerequisite. 
Gateways do not require NVIDIA drivers. Remove or correct every instance.

The correct rule is:
- **Orchestrators / GPU Nodes** → NVIDIA drivers required
- **Gateways** → No NVIDIA drivers needed

Add a callout in the gateway install prerequisites to make this explicit and prevent future confusion:

```mdx
<Note>
  Gateways do not require NVIDIA GPU drivers. GPU requirements apply to 
  Orchestrators only. See the [Orchestrator setup guide](/v2/orchestrators) 
  if you are running GPU compute.
</Note>
```

**Why this matters:** This came directly from the contributor who reviewed the section. Factual errors in install prerequisites destroy trust immediately and silently cause failed setups.

---

### 1.2 Replace Self-Maintained Docker Install Steps

**File:** `v2/gateways/run-a-gateway/install/docker-install.mdx`

**Current state:** The page walks through Docker installation steps inline.  
**Problem:** Docker's install process changes across platforms and package sources. Maintaining this in docs is a liability.

**What to do:**  
Replace the inline install steps with a reference out to the official Docker docs, plus a brief orientation callout:

```mdx
<Card 
  title="Install Docker Engine" 
  href="https://docs.docker.com/engine/install/" 
  icon="docker" 
  arrow 
  horizontal
>
  Official Docker Engine installation guide — covers Linux, Windows, and macOS.
</Card>

<Tip>
  **Windows users:** Install Docker via WSL2 for best compatibility with 
  go-livepeer. See Docker's [WSL2 backend guide](https://docs.docker.com/desktop/windows/wsl/).
</Tip>
```

Keep only any Livepeer-specific post-install steps (e.g., adding the user to the `docker` group, 
verifying the install, or confirming Docker Compose v2 is available). Those are legitimate additions. 
Generic Docker install steps are not.

---

### 1.3 Add Payment / NaaP Placeholder Sections

**Context:** eliteproxy and Qiang are building a NaaP (Network-as-a-Platform) single-click deployment 
experience. This needs a home in docs before the content exists, so the IA is correct when it arrives.

**Files to create:**

`v2/orchestrators/payments/index.mdx`  
`v2/gateways/payments/index.mdx`

Use `<ComingSoonCallout />` on both. Title them "Network Payments & Clearinghouses" or similar.

Minimal stub content for each:

```mdx
---
title: Network Payments & Clearinghouses
description: Funding your node, managing ticket payments, and payment clearinghouse integrations.
sidebarTitle: Payments
---

import { ComingSoonCallout } from '/snippets/components/domain/SHARED/previewCallouts.jsx'

<ComingSoonCallout />

This section will cover:

- Funding your Arbitrum ETH account for on-chain operation
- Probabilistic micropayment (PM) ticket system
- NaaP (Network-as-a-Platform) single-click deployment
- Payment clearinghouse integrations

For current Arbitrum exchange references, see [Arbitrum Exchange Reference](/v2/gateways/references/arbitrum-exchanges).
```

Add both stubs to `docs.json` navigation so they appear in the sidebar.

---

## Phase 2 — Orchestrator Quickstart (Docker assets now available)

Docker compose and installation assets have been provided by eliteproxy. This phase 
structures them into a working quickstart.

### 2.1 Introduce Use-Case Selection at Quickstart Entry

**File:** `v2/orchestrators/quickstart/overview.mdx` (or current entry page)

The single biggest IA change needed: orchestrator configurations are not interchangeable. 
A user setting up a transcoding orchestrator has completely different prerequisites, 
software, and configuration than one running real-time AI.

Add a use-case selector at the start of the quickstart. This can be implemented as cards or tabs:

```mdx
## What do you want to run?

<CardGroup cols={3}>
  <Card title="Transcoding" href="./transcoding-quickstart" icon="film">
    Encode and transcode video streams. GPU optional.
  </Card>
  <Card title="Realtime AI" href="./realtime-ai-quickstart" icon="bolt">
    Real-time video-to-video AI pipelines. GPU required.
  </Card>
  <Card title="Batch AI" href="./batch-ai-quickstart" icon="layer-group">
    Asynchronous AI inference jobs. GPU required.
  </Card>
</CardGroup>
```

Only **Transcoding** gets full content now. Realtime AI and Batch AI get `<ComingSoonCallout />` 
with links to the `box.md` reference for readers who are ahead of the docs.

---

### 2.2 Write the Transcoding Orchestrator Quickstart

**File to create:** `v2/orchestrators/quickstart/transcoding-quickstart.mdx`

Structure the steps as follows. eliteproxy's Docker compose slots into Step 3.

---

#### Step 1 — Create and fund your ETH account

This is the actual first entrypoint. The ETH account creation is currently missing or buried. 
It needs to be Step 1, not a reference page.

```mdx
## Step 1: Create your Ethereum account

You need an Arbitrum One ETH account to operate a node on-chain. If you already have one, 
skip to [funding it](#fund-your-account).

### Create a new keystore

```bash
# Using go-livepeer to generate a keystore
./livepeer -network arbitrum-one-mainnet -ethKeystorePath ~/.lpData/keystore
````

Follow the prompts to set a password. Store your keystore and password securely — 
this controls your node's funds and identity.

<Warning>
  Never commit your keystore or password to version control. 
  Add `~/.lpData/keystore` to your `.gitignore`.
</Warning>

### Fund your account

Your node needs ETH on Arbitrum One for gas and ticket payments.

- Minimum recommended: **0.1 ETH** to get started
- Use the [Arbitrum Bridge](https://bridge.arbitrum.io/) to move ETH from mainnet
- See [Arbitrum exchanges](/v2/gateways/references/arbitrum-exchanges) for other options
```

---

#### Step 2 — Prerequisites checklist

```mdx
## Step 2: Prerequisites

Before pulling the Docker image, confirm:

| Requirement | Notes |
|---|---|
| Docker Engine installed | [Install Docker](https://docs.docker.com/engine/install/) |
| Docker Compose v2 | Verify with `docker compose version` |
| ETH account funded | See Step 1 |
| Ports open | `8935` (orchestrator), `7935` (metrics, optional) |
| NVIDIA drivers | Required for GPU transcoding — optional for CPU-only mode |
```

---

#### Step 3 — Pull and configure

Slot eliteproxy's Docker compose here. Add annotation comments to explain each environment variable. 
Do not leave compose variables unexplained — this is where first-time operators will get lost.

Example annotation pattern:

```mdx
## Step 3: Configure and run

```yaml
# docker-compose.yml
services:
  orchestrator:
    image: livepeer/go-livepeer:latest
    environment:
      # Your funded Arbitrum ETH address
      - ETH_KEYSTORE_PATH=/keystore
      - ETH_PASSWORD_PATH=/password
      # Pricing: cost per pixel in wei. 1000 = ~$0.000001 per pixel
      - PRICE_PER_UNIT=1000
      # Max concurrent transcoding sessions
      - MAX_SESSIONS=10
      # Your publicly accessible service URI (must be reachable by gateways)
      - SERVICE_URI=https://your-orchestrator.example.com:8935
    volumes:
      - ~/.lpData/keystore:/keystore:ro
      - ~/.lpData/password:/password:ro
```
```

---

#### Step 4 — Register on-chain

```mdx
## Step 4: Register as an orchestrator

Once your node is running, activate it on-chain:

```bash
# Connect to your running node's CLI
livepeer_cli

# From the CLI menu, select: Invoke multistep wizard > Activate as an orchestrator
# Or pass flags directly:
./livepeer \
  -network arbitrum-one-mainnet \
  -blockRewardCut 5.0 \
  -feeShare 10.0 \
  -pricePerUnit 1000 \
  -pixelsPerUnit 1000000000000 \
  -serviceURI https://your-orchestrator.example.com:8935
```

<Tip>
  After activating, your orchestrator appears in the [Livepeer Explorer](https://explorer.livepeer.org) 
  within 1-2 rounds (approximately 10 minutes).
</Tip>
```

---

#### Step 5 — Verify

```mdx
## Step 5: Verify your node

Check that your orchestrator is discoverable:

```bash
# Check node health
curl http://localhost:7935/status

# Verify on-chain registration via CLI
livepeer_cli -cmd status
```

Your node should appear at:
- [Livepeer Explorer → Orchestrators](https://explorer.livepeer.org/orchestrators)
- The Livepeer Tools Dashboard (if you've configured metrics)
```

---

### 2.3 Add ETH Account Creation as a Shared Prerequisite Snippet

The ETH account creation step is relevant for both orchestrators and gateways running on-chain. 
Create it as a shared snippet rather than duplicating it:

**File:** `snippets/pages/SHARED/eth-account-setup.mdx`

Import it into both the orchestrator quickstart and the gateway on-chain setup guide.

---

## Phase 3 — Orchestrator Section Review Pass

eliteproxy has offered to review the full `v2/orchestrators` section and submit PRs. 
When those PRs arrive, your role is editorial review, not authoring.

### 3.1 What to validate in his PRs

For each page he touches, check:

- **Accuracy:** Does the flag/command/parameter match the current go-livepeer release?
- **Completeness:** Are there prerequisites or follow-on steps missing?
- **Component consistency:** Is he using the correct Mintlify callout components? (`<Note>`, `<Warning>`, `<Tip>`, `<ComingSoonCallout />`)
- **Cross-links:** Does each page link to adjacent pages correctly? (Orchestrator setup → test with gateway, gateway setup → verify with orchestrator)
- **IA fit:** Does the new content belong where he's put it, or does it need to move?

### 3.2 Pages to review independently (not waiting for his PR)

These pages exist and need an accuracy pass regardless:

| Page | Location | What to check |
|---|---|---|
| Orchestrator Architecture | `about-orchestrators/architecture` | Still accurate? Matches current go-livepeer? |
| Orchestrator Economics | `about-orchestrators/economics` | Pricing model still correct? |
| Orchestrator Functions | `about-orchestrators/orchestrator-functions` | Capabilities list complete? |
| Join a Pool | `quickstart/join-a-pool` | Still the right path for delegating to an orchestrator pool? |
| Orchestrator Setup Overview | `setting-up-an-orchestrator/overview` | Does it link correctly into the new quickstart? |

### 3.3 Add the Job Type Concept Page

**File to create:** `v2/orchestrators/about-orchestrators/job-types.mdx`

This is the explanation page that feeds the three-path quickstart. It answers: why do the three 
configuration paths exist? What's different between them?

```mdx
---
title: Types of Orchestrator Jobs
description: Understanding the difference between Transcoding, Realtime AI, and Batch AI job configurations.
sidebarTitle: Job Types
---

Livepeer Orchestrators can serve three distinct types of compute jobs, each requiring 
different configuration and (in some cases) different software.

## Transcoding

Video transcoding converts streams between formats, bitrates, and resolutions. 
This is Livepeer's original use case.

- **GPU:** Optional (CPU transcoding available, GPU strongly recommended for throughput)
- **Additional software:** None beyond go-livepeer
- **Typical use:** Live video platforms, VOD pipelines

## Realtime AI

Real-time video-to-video AI pipelines process live video streams through AI models 
(e.g., depth estimation, style transfer, segmentation) with sub-second latency requirements.

- **GPU:** Required
- **Additional software:** `mediamtx` for stream handling; see [box.md](https://github.com/livepeer/go-livepeer/blob/master/box/box.md)
- **Typical use:** Live AI video effects, interactive video

<Note>
  Realtime AI setup is more involved than transcoding. Review the 
  [box documentation](https://github.com/livepeer/go-livepeer/blob/master/box/box.md) 
  before proceeding.
</Note>

## Batch AI

Asynchronous AI inference over video or image content. Jobs are queued and processed 
without latency constraints.

- **GPU:** Required
- **Additional software:** Pipeline-dependent
- **Typical use:** Post-production AI, content analysis, large-scale inference

<ComingSoonCallout />
```

---

## Phase 4 — Gateway Section Completion

### 4.1 Slot the go-livepeer Transcoding Gateway Guide

eliteproxy is contributing a go-livepeer native transcoding gateway guide. 
This is the key content that makes the gateway section network-native rather than Studio-abstracted.

**Target location:** `v2/gateways/run-a-gateway/transcoding.mdx`

When his guide arrives, ensure:
- It is placed before any Studio-referencing guides in the sidebar
- The gateway quickstart cross-links to it as the "what happens after quickstart" path
- It uses the same Docker-link pattern established in Phase 1 (no inline install steps)

### 4.2 Establish the Orchestrator ↔ Gateway Test Loop

Currently there is no documented user journey that says: "I have an orchestrator running, 
now test it by sending it a job from a gateway." This is a critical gap — it's the end-to-end 
verification path.

**Add to:** Both the orchestrator quickstart conclusion and the gateway quickstart:

```mdx
## Testing end-to-end

Once both an Orchestrator and Gateway are running, you can verify your setup by:

1. Pointing a gateway at your orchestrator directly:
   ```bash
   -orchAddr=https://your-orchestrator.example.com:8935
   ```
2. Sending a test stream through the gateway
3. Confirming the orchestrator logs show a transcoding session

See the [Gateway Quickstart](/v2/gateways/quickstart/gateway-setup) 
for full gateway setup.
```

### 4.3 Complete the Gateway Section Stubs

From the index, the following are marked `⚠️` (incomplete or placeholder):

| Page | Status | Action |
|---|---|---|
| `about-gateways/overview.mdx` | ⚠️ | Review and complete, or remove if redundant with portal |
| `about-gateways/quickstart-home.mdx` | ⚠️ | Ensure this links cleanly to the working quickstart |
| `gateway-tools/gateway-middleware.mdx` | ⚠️ | Add or mark Coming Soon |
| `guides-and-resources/gateway-job-pipelines/byoc.mdx` | ⚠️ | BYOC content needed — flag for SME |
| `references/orchestrator-offerings.mdx` | ⚠️ | Validate protobuf fields against current go-livepeer |
| `references/configuration-flags-old.mdx` | ⚠️ | Archive or remove — superseded by `configuration-flags.mdx` |
| `references/contract-addresses.mdx` | ⚠️ | Add current Arbitrum contract addresses |

---

## Structural Note: Studio vs go-livepeer Separation

eliteproxy's most important systemic observation:

> *"IMO the Livepeer Studio guides should be their own community of practice, for Studio's own API. 
> They do not communicate network value to technically minded builders."*

This isn't a Phase 1 change — it's a longer-term IA decision — but the groundwork should be laid now.

**Immediate action:** Ensure every guide that is Studio-specific is clearly labelled as such. 
Add a callout at the top of Studio-specific pages:

```mdx
<Info>
  This guide uses the **Livepeer Studio API**, a managed service built on the Livepeer network. 
  If you want to interact with the network directly using go-livepeer, 
  see the [Gateway guide](/v2/gateways) or [Orchestrator guide](/v2/orchestrators).
</Info>
```

This makes the distinction visible without restructuring the entire nav yet.

---

## Contributor Management Checklist

For eliteproxy's incoming PRs:

```
□ Confirm he's targeting the docs-v2 branch
□ Share target file paths for his transcoding gateway guide
□ Share the callout import pattern (PreviewCallout, ComingSoonCallout, etc.)
□ Agree on scope: he authors gateway transcoding content + orchestrator review
□ You handle: IA, editorial, component consistency, cross-linking
□ Flag the python-based gateway alpha as Phase 5 (do not block current work)
□ Flag NaaP / Single-click deployment as Phase 5 (placeholder added in Phase 1)
```

---

## Priority Order Reference

| # | Action | Who | Status |
|---|---|---|---|
| 1 | Fix NVIDIA driver error in gateway install | You | Ship now |
| 2 | Replace Docker install with official link | You | Ship now |
| 3 | Add NaaP payment placeholder pages (both sections) | You | Ship now |
| 4 | Add `<Info>` Studio vs go-livepeer labels on Studio pages | You | Ship now |
| 5 | Write transcoding orchestrator quickstart scaffold | You | Docker assets in hand |
| 6 | Add ETH account creation as shared snippet | You | With #5 |
| 7 | Add use-case selector (Transcoding / Realtime AI / Batch AI) | You | With #5 |
| 8 | Create Job Types concept page | You | With #5 |
| 9 | Editorial review of eliteproxy's orchestrator PR | You (review) | Awaiting his PR |
| 10 | Slot gateway transcoding guide (go-livepeer native) | eliteproxy + You | Awaiting his PR |
| 11 | Add Orchestrator ↔ Gateway test loop to both quickstarts | You | With #10 |
| 12 | Complete remaining ⚠️ gateway stubs | You | SME input needed for BYOC |
| 13 | Python-based gateway | Future | Alpha — do not block |
| 14 | NaaP / single-click full content | Future | Awaiting eliteproxy + Qiang |
