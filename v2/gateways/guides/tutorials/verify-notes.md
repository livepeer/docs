Tutorial 1 — Off-chain transcoding (~15 min)
Full pipeline diagram, 5 steps, 12 Tips, 5 Accordion troubleshooting entries. Key accuracy decisions: correct flag (-gateway not -broadcaster — flagged explicitly in a Warning), -transcoder combined with -orchestrator explained, separate -datadir per process to avoid state conflicts, ffmpeg -re flag explained, HLS playlist URL format verified (/stream/{key}.m3u8).
Tutorial 2 — BYOC CPU pipeline (~30 min)
Covers the full BYOC architecture explanation up front (trickle HTTP protocol, not gRPC, not ai-runner), then 5 steps: PyTrickle processor code → Dockerfile (python:3.11-slim not nvidia) → isolation test with http-trickle → wire into gateway/orchestrator → optional Whisper-tiny swap. 14 Tips, 5 Accordion entries. Key accuracy: BYOC vs standard pipelines distinction explicitly stated, process() method contract explained, Whisper CPU speed (~10x realtime) documented as expected not a bug.
Tutorial 3 — Go production (30–90 min)
Three independent upgrades with a persona decision table at the top. Upgrade 1 (on-chain): ETH amounts with caveat about volatility, dedicated wallet warning, livepeer_cli deposit flow. Upgrade 2 (GPU): VRAM table by pipeline, -nvidia 0 flag, BYOC GPU container Dockerfile. Upgrade 3 (network): three discovery options (on-chain automatic, explicit list, remote signer), pricing JSON format, systemd unit file, -maxPricePerUnit confusion documented. 20 Tips across all three upgrades.
One flag to verify with Rick before publishing: -byocContainerURL and -byocModelID — these are the BYOC orchestrator flags used in Tutorial 2 Step 4b. The PR (#3866) was draft at time of research; confirm exact flag names match the merged version.
