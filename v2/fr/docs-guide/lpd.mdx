---
title: Référence de l'API CLI LPD
sidebarTitle: API CLI LPD
description: 'Guide et référence d''API pour le CLI lpd, axé sur l''utilisateur.'
keywords:
  - livepeer
  - lpd cli guide
  - internal
  - repository
  - cli
  - setup
  - local development
  - test orchestration
  - hook management
  - script execution
---
{/* codex-i18n:  */}
import { DynamicTable } from "/snippets/components/layout/table.jsx";
import { TabsContainer } from "/snippets/components/display/containers.mdx";

<Tip>
`lpd` is the repository CLI for setup, local development, test orchestration, hook management, and script execution.
Use this page as both your first-run guide and full command/flag reference.
</Tip>

## Qu'est-ce que lpd

`lpd` standardise la configuration et les opérations locales pour ce dépôt.
Il encapsule l'installation, la prévisualisation, les tests, les hooks et les scripts gérés derrière une seule interface de commande.
La source de vérité du comportement est le répertoire racine [`lpd`](/lpd) script.

## Commencez ici (chemin pour les nouveaux contributeurs)

1. Installez/initialisez l'environnement CLI du dépôt.
2. Vérifiez votre environnement et l'état des hooks.
3. Démarrer la prévisualisation locale des documents.
4. Exécutez les vérifications de base avant d'ouvrir une PR.

## Installation

Commande d'installation/initialisation standard :

```bash
bash lpd setup --yes
```

Qu'est-ce que`setup` fait :

- Installe les dépendances `tools`(`cd tools && npm install`)
- Installe les dépendances `tests`(`cd tests && npm install`)
- Installe/met à jour les hooks git (`.githooks/install.sh`)
- Installe éventuellement `lpd` dans votre PATH (sauf si`--no-cli`)

Vérifier l'installation :

```bash
lpd version
lpd doctor
```

Si le PATH n'a pas été actualisé encore :

```bash
bash lpd version
bash lpd doctor
```

Variantes courantes :

```bash
lpd setup --no-cli
lpd setup --no-tests
lpd setup --with-mintlify
LPD_INSTALL_DIR="$HOME/.local/bin" lpd setup --yes
```

<Note>
If `lpd setup` updates your shell rc file, open a new terminal or run `source ~/.zshrc` (or your active shell rc) before using plain `lpd`.
</Note>

## Flux de travail des 10 premières minutes

### Je dois juste avoir les documents en marche

```bash
bash lpd setup --yes
lpd dev -- --port 3333
```

### Je dois exécuter des vérifications avant la PR

```bash
lpd doctor --strict
lpd test --staged
lpd ci --skip-browser
```

### Je dois exécuter un script géré

```bash
lpd scripts list --group tools
lpd tools dev test-add-callouts -- --help
```

## Index des commandes

<DynamicTable
  headerList={["Command", "Flags available", "Purpose", "JSON-capable"]}
  itemsList={[
    { Command: "lpd help", "Flags available": "none", Purpose: "Root help and examples", "JSON-capable": "No" },
    { Command: "lpd info", "Flags available": "--help, -h", Purpose: "CLI purpose, command groups, and workflows", "JSON-capable": "No structured JSON output" },
    { Command: "lpd version", "Flags available": "--json (global prefix)", Purpose: "CLI version", "JSON-capable": "Yes" },
    { Command: "lpd setup", "Flags available": "--yes, --no-tools, --no-tests, --no-hooks, --with-mintlify, --no-cli, --json", Purpose: "Bootstrap dependencies, hooks, and PATH wiring", "JSON-capable": "Yes" },
    { Command: "lpd doctor", "Flags available": "--strict, --json", Purpose: "Environment and repo-readiness checks", "JSON-capable": "Yes" },
    { Command: "lpd dev", "Flags available": "--test, --test-mode, --script, --script=, --dry-run, --json, --", Purpose: "Run local docs dev launcher", "JSON-capable": "Yes" },
    { Command: "lpd mint dev", "Flags available": "-- (pass-through), --json (global prefix)", Purpose: "Alias to lpd dev", "JSON-capable": "Yes" },
    { Command: "lpd test", "Flags available": "--staged, --full, --browser, --domain, --wcag, --wcag-no-fix, --link-audit-external, --base-url, --json", Purpose: "Run tests and optional audits", "JSON-capable": "Yes" },
    { Command: "lpd ci", "Flags available": "--skip-browser, --base-url, --json", Purpose: "Run CI-like local validation", "JSON-capable": "Yes" },
    { Command: "lpd ai-sitemap", "Flags available": "--write, --check, --json", Purpose: "Generate/validate sitemap-ai.xml", "JSON-capable": "Yes" },
    { Command: "lpd hooks", "Flags available": "built-in subcommands; -- for script args; --json via finish-based paths", Purpose: "Manage hooks and run hook scripts", "JSON-capable": "Yes for finish-based paths" },
    { Command: "lpd scripts list", "Flags available": "--group, --show-ignored, --json", Purpose: "List managed scripts", "JSON-capable": "Status envelope only" },
    { Command: "lpd scripts run", "Flags available": "--dry-run, --yes, --json, --", Purpose: "Run managed scripts", "JSON-capable": "Yes" },
    { Command: "lpd <tools|tasks|tests|v2>", "Flags available": "-- (pass-through to script)", Purpose: "Shorthand for scripts run", "JSON-capable": "Yes" },
  ]}
  monospaceColumns={[0]}
/>

## Référence des commandes


<Accordion title="Root Command Details" icon="terminal">
  <ResponseField name="lpd help" type="command">
    Show root command summary and examples.

    **Usage**
    ```bash
    lpd help
    lpd --help
    lpd -h
    ```

    **Examples**
    ```bash
    lpd help
    ```
  </ResponseField>

  <ResponseField name="lpd info" type="command">
    Show CLI purpose, command groups, and common workflows.

    **Usage**
    ```bash
    lpd info
    ```

    **Examples**
    ```bash
    lpd info
    lpd info --help
    ```

    **Flags**
    <ResponseField name="--help | -h" type="boolean" default="false">
      Prints info-specific usage text.
      Example: `lpd info --help`
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd version" type="command">
    Print the active CLI version.

    **Usage**
    ```bash
    lpd version
    ```

    **Examples**
    ```bash
    lpd version
    lpd --json version
    ```
  </ResponseField>
</Accordion>

### Commandes du workflow principal

**Disponibles pour exécuter dans le workflow principal en ce moment**

<DynamicTable
  headerList={["Runnable command", "What it actually does"]}
  itemsList={[
    { "Runnable command": "lpd setup", "What it actually does": "Bootstraps tools/tests dependencies, installs hooks, and optionally installs lpd on PATH." },
    { "Runnable command": "lpd doctor", "What it actually does": "Checks git context, node/mint availability, dependency folders, and hook sync state." },
    { "Runnable command": "lpd dev", "What it actually does": "Runs the local launcher script (default: tools/scripts/mint-dev.sh) with optional pass-through args." },
    { "Runnable command": "lpd mint dev", "What it actually does": "Compatibility alias to lpd dev." },
    { "Runnable command": "lpd test", "What it actually does": "Runs tests/run-all.js plus optional browser/domain/WCAG/external-link audit flows." },
    { "Runnable command": "lpd ci", "What it actually does": "Runs CI-like local flow: staged test suite, domain audit, and optional browser integration test." },
    { "Runnable command": "lpd ai-sitemap", "What it actually does": "Runs tools/scripts/generate-ai-sitemap.js in check or write mode." },
  ]}
  monospaceColumns={[0]}
/>

<Note>
For deeper test/audit script behaviour and owning paths, see [`quality-gates`](./quality-gates.mdx) and [`workflows-index`](./workflows-index.mdx).
</Note>

<Accordion title="Core Workflow Command Details" icon="terminal">
  <ResponseField name="lpd setup" type="command">
    Bootstrap local docs environment and optional CLI PATH wiring.

    **Usage**
    ```bash
    lpd setup [--yes] [--no-tools] [--no-tests] [--no-hooks] [--with-mintlify] [--no-cli] [--json]
    ```

    **Examples**
    ```bash
    bash lpd setup --yes
    lpd setup --no-tests --no-cli
    lpd setup --with-mintlify
    ```

    **Flags**
    <ResponseField name="--yes" type="boolean" default="false">
      Runs setup non-interactively with default decisions.
    </ResponseField>
    <ResponseField name="--no-tools" type="boolean" default="false">
      Skips `cd tools && npm install`.
    </ResponseField>
    <ResponseField name="--no-tests" type="boolean" default="false">
      Skips `cd tests && npm install`.
    </ResponseField>
    <ResponseField name="--no-hooks" type="boolean" default="false">
      Skips `.githooks/install.sh`.
    </ResponseField>
    <ResponseField name="--with-mintlify" type="boolean" default="false">
      Installs Mintlify globally via `npm i -g mintlify`.
    </ResponseField>
    <ResponseField name="--no-cli" type="boolean" default="false">
      Skips installing `lpd` symlink/PATH wiring.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output.
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd doctor" type="command">
    Check git context, runtime tools, dependency folders, and hook sync state.

    **Usage**
    ```bash
    lpd doctor [--strict] [--json]
    ```

    **Examples**
    ```bash
    lpd doctor
    lpd doctor --strict
    lpd --json doctor
    ```

    **Flags**
    <ResponseField name="--strict" type="boolean" default="false">
      Fails with non-zero exit if any issue is found.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output.
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd dev" type="command">
    Run the local docs dev launcher script.

    **Usage**
    ```bash
    lpd dev [--test] [--test-mode fast|staged|full] [--script <path>] [--script=<path>] [--dry-run] [--json] [-- ...mint args]
    ```

    **Examples**
    ```bash
    lpd dev
    lpd dev --test --test-mode staged
    lpd dev --dry-run -- --port 3333
    lpd dev --script tools/scripts/mint-dev.sh -- --port 3001
    ```

    **Flags**
    <ResponseField name="--test" type="boolean" default="false">
      Runs advisory tests before starting the dev server.
    </ResponseField>
    <ResponseField name="--test-mode fast|staged|full" type="enum" default="fast">
      Selects advisory test mode used with `--test`.
    </ResponseField>
    <ResponseField name="--script <path> | --script=<path>" type="string" default="tools/scripts/mint-dev.sh">
      Overrides the launcher script path (absolute or repo-relative).
    </ResponseField>
    <ResponseField name="--dry-run" type="boolean" default="false">
      Prints the resolved launcher command and exits.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output.
    </ResponseField>
    <ResponseField name="--" type="separator">
      Passes remaining args to the launcher script.
      Example: `lpd dev -- --port 3333`
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd mint dev" type="command">
    Compatibility alias that forwards directly to `lpd dev`.

    **Usage**
    ```bash
    lpd mint dev [-- ...mint args]
    ```

    **Examples**
    ```bash
    lpd mint dev
    lpd mint dev -- --port 3333
    ```
  </ResponseField>

  <ResponseField name="lpd test" type="command">
    Run test suite modes with optional browser/domain/WCAG/external-link checks.

    **Usage**
    ```bash
    lpd test [--staged|--full] [--browser] [--domain v1|v2|both] [--wcag] [--wcag-no-fix] [--link-audit-external] [--base-url URL] [--json]
    ```

    **Examples**
    ```bash
    lpd test --staged
    lpd test --staged --wcag
    lpd test --full --link-audit-external
    lpd test --domain both --base-url http://localhost:3000
    lpd test --browser
    ```

    **Flags**
    <ResponseField name="--staged" type="boolean" default="false">
      Sets test mode to staged.
    </ResponseField>
    <ResponseField name="--full" type="boolean" default="false">
      Sets test mode to full.
    </ResponseField>
    <ResponseField name="--browser" type="boolean" default="false">
      Runs browser integration test (`tests/integration/browser.test.js`).
    </ResponseField>
    <ResponseField name="--domain v1|v2|both" type="enum" default="unset">
      Runs domain-pages audit for the selected scope.
    </ResponseField>
    <ResponseField name="--wcag" type="boolean" default="false">
      Runs WCAG audit (staged-capped unless mode is full).
    </ResponseField>
    <ResponseField name="--wcag-no-fix" type="boolean" default="false">
      Runs WCAG audit in suggestion-only mode (`--no-fix`).
    </ResponseField>
    <ResponseField name="--link-audit-external" type="boolean" default="false">
      Runs external link audit in navigational validation mode.
    </ResponseField>
    <ResponseField name="--base-url URL" type="string" default="unset">
      Forwards a custom base URL to supported audits.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output.
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd ci" type="command">
    Run local CI-like validation flow.

    **Usage**
    ```bash
    lpd ci [--skip-browser] [--base-url URL] [--json]
    ```

    **Examples**
    ```bash
    lpd ci
    lpd ci --skip-browser
    lpd ci --base-url http://localhost:3000
    ```

    **Flags**
    <ResponseField name="--skip-browser" type="boolean" default="false">
      Skips browser integration step.
    </ResponseField>
    <ResponseField name="--base-url URL" type="string" default="unset">
      Forwards base URL to domain-pages audit.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output.
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd ai-sitemap" type="command">
    Generate or validate `sitemap-ai.xml`.

    **Usage**
    ```bash
    lpd ai-sitemap [--write|--check] [--json]
    ```

    **Examples**
    ```bash
    lpd ai-sitemap --check
    lpd ai-sitemap --write
    lpd --json ai-sitemap --check
    ```

    **Flags**
    <ResponseField name="--write" type="boolean" default="false">
      Runs generator in write mode.
    </ResponseField>
    <ResponseField name="--check" type="boolean" default="true">
      Runs generator in check mode.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output.
    </ResponseField>
  </ResponseField>
</Accordion>

### Commandes des hooks

**Disponibles pour exécuter dans les hooks en ce moment**

<DynamicTable
  headerList={["Runnable hooks command", "Flags available", "How to run", "PURPOSE"]}
  itemsList={[
    { "Runnable hooks command": "lpd hooks install", "Flags available": "none", "How to run": "Built-in hooks subcommand", PURPOSE: "Copies/syncs hook files into .git/hooks so repository checks run on commit." },
    { "Runnable hooks command": "lpd hooks status", "Flags available": "none", "How to run": "Built-in hooks subcommand", PURPOSE: "Validates current hook state and reports drift (missing, non-executable, out-of-date)." },
    { "Runnable hooks command": "lpd hooks verify", "Flags available": "none", "How to run": "Built-in hooks subcommand", PURPOSE: "Executes hook health checks to confirm required hook scripts and policy wiring are valid." },
    { "Runnable hooks command": "lpd hooks info", "Flags available": "none", "How to run": "Built-in hooks subcommand", PURPOSE: "Shows operator guidance, bypass env flags, and human-only override trailer rules." },
  ]}
  monospaceColumns={[0]}
/>

Vous pouvez également exécuter directement les scripts de hook gérés via la résolution de secours :

<DynamicTable
  headerList={["Hook script command token", "Flags available", "Resolved script path", "PURPOSE"]}
  itemsList={[
    { "Hook script command token": "lpd hooks install", "Flags available": "script-specific via `-- <args>`", "Resolved script path": ".githooks/install.sh", PURPOSE: "Installs or refreshes git hook files from repository-managed hook sources." },
    { "Hook script command token": "lpd hooks verify-browser", "Flags available": "script-specific via `-- <args>`", "Resolved script path": ".githooks/verify-browser.js", PURPOSE: "Runs browser-focused verification used by hook workflows." },
    { "Hook script command token": "lpd hooks server-manager", "Flags available": "script-specific via `-- <args>`", "Resolved script path": ".githooks/server-manager.js", PURPOSE: "Manages local server lifecycle steps used by hook/browser checks." },
    { "Hook script command token": "lpd hooks pre-commit", "Flags available": "script-specific via `-- <args>`", "Resolved script path": ".githooks/pre-commit", PURPOSE: "Runs the main repository pre-commit policy and staged-check pipeline." },
    { "Hook script command token": "lpd hooks pre-commit-no-deletions", "Flags available": "script-specific via `-- <args>`", "Resolved script path": ".githooks/pre-commit-no-deletions", PURPOSE: "Runs stricter pre-commit flow that blocks file deletions by policy." },
    { "Hook script command token": "lpd hooks verify", "Flags available": "script-specific via `-- <args>`", "Resolved script path": ".githooks/verify.sh", PURPOSE: "Performs shell/MDX/link/import verification checks for hook integrity." },
  ]}
  monospaceColumns={[0, 2]}
/>

<DynamicTable
  headerList={["Discovery command", "Flags available", "What you get", "PURPOSE"]}
  itemsList={[
    { "Discovery command": "lpd hooks info", "Flags available": "none", "What you get": "Built-in hook subcommands, policy details, and known hook scripts.", PURPOSE: "Gives the authoritative hooks runbook before you execute or bypass anything." },
    { "Discovery command": "lpd scripts list --group hooks --show-ignored", "Flags available": "--group, --show-ignored, --json", "What you get": "Hook-group script inventory, including ignored entries.", PURPOSE: "Shows every hook command token you can invoke via script runner fallback." },
  ]}
  monospaceColumns={[0]}
/>

<Accordion title="Hooks Command Details" icon="terminal">
  <ResponseField name="lpd hooks" type="command">
    Manage pre-commit hooks or run managed scripts under `.githooks/`.

    **Usage**
    ```bash
    lpd hooks <install|status|verify|info|<hook-script tokens...>> [-- script args]
    ```

    **Examples**
    ```bash
    lpd hooks install
    lpd hooks status
    lpd hooks verify
    lpd hooks info
    lpd hooks verify-browser --dry-run
    ```

    **Subcommands**
    <ResponseField name="install" type="subcommand">
      Installs/updates `.git/hooks/pre-commit` from `.githooks/pre-commit`.
    </ResponseField>
    <ResponseField name="status" type="subcommand">
      Checks hook exists, is executable, and matches source.
    </ResponseField>
    <ResponseField name="verify" type="subcommand">
      Runs verification checks via `.githooks/verify.sh`.
    </ResponseField>
    <ResponseField name="info" type="subcommand">
      Prints hook scripts, bypass flags, and policy references.
    </ResponseField>
    <ResponseField name="<hook-script tokens...>" type="subcommand">
      Fallback path: unresolved hooks subcommands are treated as managed script tokens in `.githooks/`.
    </ResponseField>
  </ResponseField>
</Accordion>

<Note>
Human-only override trailers:
`git commit -m "msg" --trailer "allowlist-edit=true"`
and
`git commit -m "msg" --trailer "allow-deletions=true"`.
</Note>

### Commandes des scripts

Utilisez ces commandes d'entrée pour découvrir et exécuter des scripts :

<DynamicTable
  headerList={["Action", "Flags available", "Run this", "PURPOSE"]}
  itemsList={[
    { Action: "List every script token", "Flags available": "--group, --show-ignored, --json", "Run this": "lpd scripts list", PURPOSE: "Shows all runnable script tokens across tools/tasks/tests/v2/hooks with risk and path." },
    { Action: "List one group", "Flags available": "--group, --show-ignored, --json", "Run this": "lpd scripts list --group <tools|tasks|tests|v2|hooks>", PURPOSE: "Shows the exact token names available in a single group." },
    { Action: "Run one script by group + token path", "Flags available": "--dry-run, --yes, --json, --", "Run this": "lpd scripts run <group> <script-path tokens...> [-- script args]", PURPOSE: "Resolves token path to a file, applies risk confirmation, then executes it." },
    { Action: "Run with shorthand alias", "Flags available": "-- (pass-through to script)", "Run this": "lpd <tools|tasks|tests|v2> <script-path tokens...> [-- script args]", PURPOSE: "Shortcut wrapper around `lpd scripts run` for each group." },
  ]}
  monospaceColumns={[2]}
/>

<Accordion title="Scripts Command Details" icon="terminal">
  <ResponseField name="lpd scripts list" type="command">
    List managed scripts across `tools`, `tasks`, `tests`, `v2`, and `hooks` groups.

    **Usage**
    ```bash
    lpd scripts list [--group tools|tasks|tests|v2|hooks] [--show-ignored] [--json]
    ```

    **Examples**
    ```bash
    lpd scripts list
    lpd scripts list --group tools
    lpd scripts list --group hooks --show-ignored
    lpd --json scripts list --group tests
    ```

    **Flags**
    <ResponseField name="--group tools|tasks|tests|v2|hooks" type="enum" default="all groups">
      Restricts output to one script group.
    </ResponseField>
    <ResponseField name="--show-ignored" type="boolean" default="false">
      Includes scripts ignored by `.lpdignore`.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output (not full structured list payload).
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd scripts run" type="command">
    Resolve and execute a managed script command in a script group.

    **Usage**
    ```bash
    lpd scripts run <group> <script-path tokens...> [--dry-run] [--yes] [--json] [-- script args]
    ```

    **Examples**
    ```bash
    lpd scripts run tools dev test-add-callouts -- --help
    lpd scripts run tasks run-audit --dry-run
    lpd scripts run tasks run-audit --yes
    ```

    **Inputs**
    <ResponseField name="<group>" type="enum" required>
      One of `tools`, `tasks`, `tests`, `v2`, or `hooks`.
    </ResponseField>
    <ResponseField name="<script-path tokens...>" type="string" required>
      Space-delimited command path that resolves to a discovered script.
    </ResponseField>

    **Flags**
    <ResponseField name="--dry-run" type="boolean" default="false">
      Prints the resolved executable command and exits.
    </ResponseField>
    <ResponseField name="--yes" type="boolean" default="false">
      Auto-confirms high-risk script groups.
    </ResponseField>
    <ResponseField name="--json" type="boolean" default="false">
      Enables JSON status envelope output.
    </ResponseField>
    <ResponseField name="--" type="separator">
      Passes remaining args to the resolved script target.
      Example: `lpd scripts run tools dev test-add-callouts -- --help`
    </ResponseField>
  </ResponseField>

  <ResponseField name="lpd <tools|tasks|tests|v2>" type="command">
    Group shorthand alias for `lpd scripts run <group> ...`.

    **Usage**
    ```bash
    lpd tools <script-path tokens...> [-- script args]
    lpd tasks <script-path tokens...> [-- script args]
    lpd tests <script-path tokens...> [-- script args]
    lpd v2 <script-path tokens...> [-- script args]
    ```

    **Examples**
    ```bash
    lpd tools dev test-add-callouts -- --help
    lpd v2 wcag-repair-common -- --staged --stage
    ```
  </ResponseField>

  <ResponseField name="Script risk model" type="command">
    Scripts are assigned risk by group and may require confirmation.

    **Usage**
    ```bash
    low: tests
    medium: tools, hooks
    high: tasks, v2
    ```

    **Examples**
    ```bash
    lpd scripts run tasks run-audit
    lpd scripts run tasks run-audit --yes
    ```

    High-risk groups prompt for confirmation unless `--yes` is supplied.
    In non-interactive mode, high-risk commands fail without `--yes`.
  </ResponseField>
</Accordion>

## Index des scripts
  <Note>
  For generated full script metadata (script path, summary, usage, owner), see [`scripts-index`](./scripts-index.md).
  </Note>

<TabsContainer>
  <Tab title="Tools">
  #### Tools Scripts: `tools` (42)

  | Script token | Flags available | Resolved path | PURPOSE |
  |---|---|---|---|
  | `audit-all-pages-simple` | script-specific via `-- <args>` | `tools/scripts/audit-all-pages-simple.js` | Utility script for tasks/scripts/audit-all-pages-simple.js. |
  | `audit-all-pages` | script-specific via `-- <args>` | `tools/scripts/audit-all-pages.js` | Utility script for tasks/scripts/audit-all-pages.js. |
  | `audit-all-v2-pages` | script-specific via `-- <args>` | `tools/scripts/audit-all-v2-pages.js` | Utility script for tools/scripts/audit-all-v2-pages.js. |
  | `audit-scripts` | script-specific via `-- <args>` | `tools/scripts/audit-scripts.js` | Audit full-repo executable scripts, categorize usage/overlap, and overwrite SCRIPT_AUDIT reports. |
  | `audit-tasks-folders` | script-specific via `-- <args>` | `tools/scripts/audit-tasks-folders.js` | Audit tasks folders, optionally normalise report locations, and optionally apply audit recommendations with conflict-safe moves. |
  | `audit-v2-usefulness` | script-specific via `-- <args>` | `tools/scripts/audit-v2-usefulness.js` | Audit v2 MDX pages (excluding x-* directories) and emit page-level usefulness matrix rows with source-weighted 2026 accuracy verification fields. |
  | `convert-rss-to-mdx` | script-specific via `-- <args>` | `tools/scripts/convert-rss-to-mdx.js` | Runs `tools/scripts/convert-rss-to-mdx.js`. |
  | `debug-mint-dev` | script-specific via `-- <args>` | `tools/scripts/debug-mint-dev.js` | Utility script for tools/scripts/debug-mint-dev.js. |
  | `deprecated project-management-output-script` | script-specific via `-- <args>` | `tools/scripts/deprecated/project-management-output-script.js` | Deprecated legacy project-management output script retained as a reference stub. |
  | `dev add-callouts` | script-specific via `-- <args>` | `tools/scripts/dev/add-callouts.js` | Utility script for tools/scripts/dev/add-callouts.js. |
  | `dev batch-update-og-image` | script-specific via `-- <args>` | `tools/scripts/dev/batch-update-og-image.sh` | Utility script for tools/scripts/dev/batch-update-og-image.sh. |
  | `dev ensure-mint-watcher-patch` | script-specific via `-- <args>` | `tools/scripts/dev/ensure-mint-watcher-patch.sh` | Ensure Mint local-preview watcher disables glob expansion in repo paths. |
  | `dev replace-og-image` | script-specific via `-- <args>` | `tools/scripts/dev/replace-og-image.py` | Utility script for tools/scripts/dev/replace-og-image.py. |
  | `dev test-add-callouts` | script-specific via `-- <args>` | `tools/scripts/dev/test-add-callouts.js` | Utility script for tools/scripts/dev/test-add-callouts.js. |
  | `download-linkedin-video` | script-specific via `-- <args>` | `tools/scripts/download-linkedin-video.sh` | Utility script for tools/scripts/download-linkedin-video.sh. |
  | `download-linkedin-with-cookies` | script-specific via `-- <args>` | `tools/scripts/download-linkedin-with-cookies.sh` | Utility script for tools/scripts/download-linkedin-with-cookies.sh. |
  | `generate-ai-sitemap` | script-specific via `-- <args>` | `tools/scripts/generate-ai-sitemap.js` | Generate an AI-focused sitemap from v2 docs navigation. |
  | `generate-docs-guide-indexes` | script-specific via `-- <args>` | `tools/scripts/generate-docs-guide-indexes.js` | Generate docs-guide workflow/template indexes and optionally verify they are up to date. |
  | `generate-docs-guide-pages-index` | script-specific via `-- <args>` | `tools/scripts/generate-docs-guide-pages-index.js` | Generate docs-guide/pages-index.mdx from v2/index.mdx entries filtered to docs.json navigation pages. |
  | `generate-docs-index` | script-specific via `-- <args>` | `tools/scripts/generate-docs-index.js` | Generate docs-index.json and optionally backfill v2 frontmatter metadata. |
  | `generate-llms-files` | script-specific via `-- <args>` | `tools/scripts/generate-llms-files.js` | Generate llms.txt and llms-full.txt from v2 docs navigation. |
  | `generate-pages-index` | script-specific via `-- <args>` | `tools/scripts/generate-pages-index.js` | Generate and verify section-style index.mdx files for v2 docs folders, plus the root aggregate index. |
  | `mint-dev` | script-specific via `-- <args>` | `tools/scripts/mint-dev.sh` | Utility script for tools/scripts/mint-dev.sh. |
  | `new-script` | script-specific via `-- <args>` | `tools/scripts/new-script.js` | Create a new script file prefilled with the required docs header template. |
  | `publish-v2-internal-reports` | script-specific via `-- <args>` | `tools/scripts/publish-v2-internal-reports.js` | Duplicate approved markdown reports into v2/internal/reports pages with metadata and update docs.json. |
  | `snippets fetch-external-docs` | script-specific via `-- <args>` | `tools/scripts/snippets/fetch-external-docs.sh` | Utility script for tools/scripts/snippets/fetch-external-docs.sh. |
  | `snippets fetch-lpt-exchanges` | script-specific via `-- <args>` | `tools/scripts/snippets/fetch-lpt-exchanges.sh` | Utility script for tools/scripts/snippets/fetch-lpt-exchanges.sh. |
  | `snippets fetch-openapi-specs` | script-specific via `-- <args>` | `tools/scripts/snippets/fetch-openapi-specs.sh` | Utility script for tools/scripts/snippets/fetch-openapi-specs.sh. |
  | `snippets generate-api-docs` | script-specific via `-- <args>` | `tools/scripts/snippets/generate-api-docs.sh` | Utility script for tools/scripts/snippets/generate-api-docs.sh. |
  | `snippets generate-data scripts generate-glossary` | script-specific via `-- <args>` | `tools/scripts/snippets/generate-data/scripts/generate-glossary.js` | Utility script for tools/scripts/snippets/generate-data/scripts/generate-glossary.js. |
  | `snippets generate-data scripts terminology-search` | script-specific via `-- <args>` | `tools/scripts/snippets/generate-data/scripts/terminology-search.js` | Utility script for tools/scripts/snippets/generate-data/scripts/terminology-search.js. |
  | `snippets generate-seo` | script-specific via `-- <args>` | `tools/scripts/snippets/generate-seo.js` | Utility script for tools/scripts/snippets/generate-seo.js. |
  | `snippets test-scripts` | script-specific via `-- <args>` | `tools/scripts/snippets/test-scripts.sh` | Utility script for tools/scripts/snippets/test-scripts.sh. |
  | `snippets update-component-library` | script-specific via `-- <args>` | `tools/scripts/snippets/update-component-library.sh` | Utility script for tools/scripts/snippets/update-component-library.sh. |
  | `test-all-pages-browser` | script-specific via `-- <args>` | `tools/scripts/test-all-pages-browser.js` | Utility script for tools/scripts/test-all-pages-browser.js. |
  | `test-all-pages-comprehensive` | script-specific via `-- <args>` | `tools/scripts/test-all-pages-comprehensive.js` | Utility script for tools/scripts/test-all-pages-comprehensive.js. |
  | `test-v2-pages` | script-specific via `-- <args>` | `tools/scripts/test-v2-pages.js` | Utility script for tools/scripts/test-v2-pages.js. |
  | `test-youtube-pages` | script-specific via `-- <args>` | `tools/scripts/test-youtube-pages.js` | Utility script for tools/scripts/test-youtube-pages.js. |
  | `test test-youtube-pages` | script-specific via `-- <args>` | `tools/scripts/test/test-youtube-pages.js` | Utility script for tools/scripts/test/test-youtube-pages.js. |
  | `transcribe-audio-to-mdx` | script-specific via `-- <args>` | `tools/scripts/transcribe-audio-to-mdx.js` | Download audio from URL, chunk with ffmpeg, transcribe via OpenRouter, and write an MDX transcript page. |
  | `verify .verify-large-change` | script-specific via `-- <args>` | `tools/scripts/verify/.verify-large-change.sh` | Reserved verifier hook placeholder for large-change checks. |
  | `wcag-repair-common` | script-specific via `-- <args>` | `tools/scripts/wcag-repair-common.js` | Apply conservative WCAG-related source autofixes across v2 docs (common raw-tag issues) and write deterministic repair reports. |
  </Tab>
  <Tab title="Tasks">
  #### Tasks Scripts: `tasks` (3)

  | Script token | Flags available | Resolved path | PURPOSE |
  |---|---|---|---|
  | `audit-minimal` | script-specific via `-- <args>` | `tasks/scripts/audit-minimal.js` | Utility script for tasks/scripts/audit-minimal.js. |
  | `audit-python` | script-specific via `-- <args>` | `tasks/scripts/audit-python.py` | Utility script for tasks/scripts/audit-python.py. |
  | `run-audit-now` | script-specific via `-- <args>` | `tasks/scripts/run-audit-now.js` | Utility script for tasks/scripts/run-audit-now.js. |

  </Tab>
  <Tab title="Tests">
  #### Test Scripts: `tests` (23)

  | Script token | Flags available | Resolved path | PURPOSE |
  |---|---|---|---|
  | `integration browser.test` | script-specific via `-- <args>` | `tests/integration/browser.test.js` | Utility script for tests/integration/browser.test.js. |
  | `integration domain-pages-audit` | script-specific via `-- <args>` | `tests/integration/domain-pages-audit.js` | Audit deployed docs page load status and emit a stable JSON report. |
  | `integration v2-link-audit` | script-specific via `-- <args>` | `tests/integration/v2-link-audit.js` | Comprehensive V2 MDX link audit with internal strict checks and optional external URL validation. |
  | `integration v2-link-audit.selftest` | script-specific via `-- <args>` | `tests/integration/v2-link-audit.selftest.js` | Script-level self-tests for v2 link audit external validation using a local HTTP fixture and temporary MDX file. |
  | `integration v2-wcag-audit` | script-specific via `-- <args>` | `tests/integration/v2-wcag-audit.js` | Audit v2 docs.json navigation pages for accessibility (WCAG 2.2 AA) with deterministic reports and conservative source autofixes. |
  | `integration v2-wcag-audit.selftest` | script-specific via `-- <args>` | `tests/integration/v2-wcag-audit.selftest.js` | Script-level self-tests for the v2 WCAG audit (local HTTP + Puppeteer axe run, and temp-file fix/stage behaviour without Mintlify). |
  | `run-all` | script-specific via `-- <args>` | `tests/run-all.js` | Utility script for tests/run-all.js. |
  | `run-pr-checks` | script-specific via `-- <args>` | `tests/run-pr-checks.js` | Run changed-file scoped validation checks for pull request CI. |
  | `unit docs-guide-sot.test` | script-specific via `-- <args>` | `tests/unit/docs-guide-sot.test.js` | Validate docs-guide source-of-truth coverage, README pointers, and generated index freshness. |
  | `unit docs-navigation.test` | script-specific via `-- <args>` | `tests/unit/docs-navigation.test.js` | Validate docs.json page-entry syntax, report missing routes, suggest remaps, and optionally apply approved remaps. |
  | `unit docs-usefulness-accuracy-verifier.test` | script-specific via `-- <args>` | `tests/unit/docs-usefulness-accuracy-verifier.test.js` | Validate source-weighted 2026 accuracy verification rules (GitHub vs DeepWiki precedence, freshness, fallback, and cache reuse). |
  | `unit links-imports.test` | script-specific via `-- <args>` | `tests/unit/links-imports.test.js` | Utility script for tests/unit/links-imports.test.js. |
  | `unit mdx-guards.test` | script-specific via `-- <args>` | `tests/unit/mdx-guards.test.js` | Enforce MDX guardrails for globals imports, math delimiters, and markdown table line breaks. |
  | `unit mdx.test` | script-specific via `-- <args>` | `tests/unit/mdx.test.js` | Utility script for tests/unit/mdx.test.js. |
  | `unit quality.test` | script-specific via `-- <args>` | `tests/unit/quality.test.js` | Utility script for tests/unit/quality.test.js. |
  | `unit script-docs.test` | script-specific via `-- <args>` | `tests/unit/script-docs.test.js` | Enforce script header schema, keep group script indexes in sync, and build aggregate script index. |
  | `unit spelling.test` | script-specific via `-- <args>` | `tests/unit/spelling.test.js` | Utility script for tests/unit/spelling.test.js. |
  | `unit style-guide.test` | script-specific via `-- <args>` | `tests/unit/style-guide.test.js` | Utility script for tests/unit/style-guide.test.js. |
  | `unit v2-link-audit.test` | script-specific via `-- <args>` | `tests/unit/v2-link-audit.test.js` | Unit tests for v2 link audit args, external validation helpers, and x-* scope exclusion behaviour. |
  | `unit v2-wcag-audit.test` | script-specific via `-- <args>` | `tests/unit/v2-wcag-audit.test.js` | Unit tests for v2 WCAG audit helper logic (args, thresholds, route mapping, report sorting, and conservative autofixes). |
  | `utils file-walker` | script-specific via `-- <args>` | `tests/utils/file-walker.js` | Utility script for tests/utils/file-walker.js. |
  | `utils mdx-parser` | script-specific via `-- <args>` | `tests/utils/mdx-parser.js` | Utility script for tests/utils/mdx-parser.js. |
  | `utils spell-checker` | script-specific via `-- <args>` | `tests/utils/spell-checker.js` | Utility script for tests/utils/spell-checker.js. |

  </Tab>
  <Tab title="Other">
  #### Other Scripts: `v2` (5)

  | Script token | Flags available | Resolved path | PURPOSE |
  |---|---|---|---|
  | `add-callouts` | script-specific via `-- <args>` | `tools/scripts/dev/add-callouts.js` | Utility script for tools/scripts/dev/add-callouts.js. |
  | `batch-update-og-image` | script-specific via `-- <args>` | `tools/scripts/dev/batch-update-og-image.sh` | Utility script for tools/scripts/dev/batch-update-og-image.sh. |
  | `ensure-mint-watcher-patch` | script-specific via `-- <args>` | `tools/scripts/dev/ensure-mint-watcher-patch.sh` | Ensure Mint local-preview watcher disables glob expansion in repo paths. |
  | `replace-og-image` | script-specific via `-- <args>` | `tools/scripts/dev/replace-og-image.py` | Utility script for tools/scripts/dev/replace-og-image.py. |
  | `test-add-callouts` | script-specific via `-- <args>` | `tools/scripts/dev/test-add-callouts.js` | Utility script for tools/scripts/dev/test-add-callouts.js. |

  </Tab>
</TabsContainer>


## Dépannage

### `lpd`non trouvé

Utilisez la solution de secours locale au dépôt et vérifiez l'installation :

```bash
bash lpd setup --yes
bash lpd version
```

Si nécessaire, rechargez votre fichier rc de shell (`source ~/.zshrc` ou équivalent).

### Mauvaise correspondance des hooks

Vérifiez et réinstallez les hooks :

```bash
lpd hooks status
lpd hooks install
lpd hooks verify
```

### `--json`attentes

Le mode JSON global est sous forme de préfixe : `lpd --json <command> ...`.
Certaines commandes acceptent également le `--json`.`lpd info --json` est invalide car `info` n'accepte que `--help|-h`.

### Conflit de port sur `lpd dev`

Transmettez un port via le lanceur :

```bash
lpd dev -- --port 3334
```

## Sortie JSON

Lorsque le mode JSON est actif sur les commandes basées sur la fin, la sortie utilise cette enveloppe de statut :

<Accordion title="JSON Output Envelope" icon="code">
  <ResponseField name="ok" type="boolean">
    `true` for success and `false` for failure.
  </ResponseField>

  <ResponseField name="command" type="string">
    Normalised command label (for example: `test`, `scripts list`, `hooks status`).
  </ResponseField>

  <ResponseField name="message" type="string">
    Human-readable summary of the command result.
  </ResponseField>

  <ResponseField name="exit_code" type="number">
    Exit code returned by the command path.
  </ResponseField>

  <ResponseField name="repo_root" type="string">
    Absolute repo root used during command execution.
  </ResponseField>

  Example:

  ```json
  {
    "ok": true,
    "command": "version",
    "message": "lpd 0.2.0",
    "exit_code": 0,
    "repo_root": "/path/to/livepeer-docs-repo"
  }
  ```

  <Note>
  `lpd info` does not emit a structured JSON envelope.
  Use JSON mode with commands that complete via `finish()`.
  </Note>

</Accordion>
