---
title: PHP
description: "Learn how to create your first stream using the Livepeer PHP SDK."
---

## Prerequisites

To get the most out of this guide, you’ll need to:

- [Create an API key](https://livepeer.studio/dashboard/developers/api-keys)

## Install the SDK

Get the Livepeer PHP SDK.

```
composer require livepeer/livepeer-php
```

## Create a stream

Create a new stream using the Livepeer PHP SDK.

```php
<?php

require 'vendor/autoload.php';

use Livepeer\Livepeer;
use Livepeer\LivepeerException;

// Initialize the Livepeer client with your API key
$api_key = 'YOUR_API_KEY';
$livepeer = new Livepeer($api_key);

$stream_data = [
  "name" => "test_stream"
];

try {
    // Create the stream
    $response = $livepeer->createStream($stream_data);
    echo 'Stream created: ' . json_encode($response) . PHP_EOL;
} catch (LivepeerException $e) {
    echo 'Error creating stream: ' . $e->getMessage() . PHP_EOL;
}

```

## Next steps

Checkout Livepeer [API Reference](/api-reference) to learn more about the Livepeer API and the PHP SDK.
