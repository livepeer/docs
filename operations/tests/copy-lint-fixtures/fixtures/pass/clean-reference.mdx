---
title: 'Pool Worker Reference'
description: 'Reference flags for connecting a worker to an existing Orchestrator pool.'
pageType: reference
audience: orchestrator
purpose: reference
status: current
lastVerified: '2026-03-15'
---

Use `-orchAddr` to point the worker at the pool Orchestrator.
Use `-serviceAddr` to advertise the worker endpoint to the pool dashboard.
Use `-maxSessions` to cap concurrent transcoding sessions on each worker.
