---
title: 'Update Review Template'
kind: 'workflow'
status: 'active'
concern: 'review'
source_path: '.github/workflows/update-review-template.yml'
trigger_mode: 'workflow_dispatch'
risk_level: 'low'
consolidation_flag: 'review for alignment with `page-ship` while preserving current workflow_dispatch trigger behavior'
dispatcher_candidate: 'page-ship'
workflow_family: 'placeholder-backlog'
cleanup_decision: 'retire'
usage_status: 'placeholder'
process_fit: 'legacy-or-unclear'
consolidation_target: 'none'
last_verified: '2026-04-05'
---

{/*
generated-file-banner: ai-tools-visual-library:v1
Generation Script: operations/scripts/generators/governance/catalogs/generate-ai-tools-visual-library.js
Purpose: AI-tools canonical visual library for workflows and dispatcher actions.
Run when: GitHub workflows, dispatcher definitions, registry coverage, or visual-library contracts change.
Run command: node operations/scripts/generators/governance/catalogs/generate-ai-tools-visual-library.js --write
*/}

<Note>
**Generation Script**: This file is generated from script(s): `operations/scripts/generators/governance/catalogs/generate-ai-tools-visual-library.js`. <br/>
**Purpose**: AI-tools canonical visual library for workflows and dispatcher actions. <br/>
**Run when**: GitHub workflows, dispatcher definitions, registry coverage, or visual-library contracts change. <br/>
**Important**: Do not manually edit this file; run `node operations/scripts/generators/governance/catalogs/generate-ai-tools-visual-library.js --write`. <br/>
</Note>

# Update Review Template

## Summary

Update Review Template runs on workflow_dispatch and primarily produces generated or refreshed repository data.

## Why It Exists

Govern the `.github/workflows/update-review-template.yml` workflow as a human-readable, visually explorable source-of-truth page inside `ai-tools/registry/workflows`.

## Triggers

- workflow_dispatch: default event configuration

## Jobs

| Job ID | Name | Runs On | Needs | Step Count |
| --- | --- | --- | --- | --- |
| `update-review-template` | update-review-template | `ubuntu-latest` | none | 1 |

### update-review-template

- `Placeholder` | runs `echo "Not yet implemented - see update-review-template.yml in backlog"`

## Inputs

- No explicit workflow inputs declared.

## Second Pass Assessment

- Workflow family: `placeholder-backlog`
- Usage status: `placeholder`
- Cleanup decision: `retire`
- Process fit: `legacy-or-unclear`
- Consolidation target: `none`
- Recommended engineering action: Retire the placeholder workflow file unless a concrete review-template pipeline is actually needed.

## Outputs

- Generated or refreshed repository data

## Dependencies

- No direct dependencies identified in current repo scan.

## Dependants

- dispatcher:page-ship

## Mermaid Pipeline

```mermaid
flowchart TD
  trigger["Trigger: workflow_dispatch"]
  workflow["Workflow: Update Review Template"]
  trigger --> workflow
  job1["Job: update-review-template"]
  workflow --> job1
  outputs["Outputs: Generated or refreshed repository data"]
  job1 --> outputs
```

## Frailty And Risk

- No local repo dependencies were detected automatically; verify whether this is truly standalone.

## Consolidation Notes

Dispatcher suggestion: `page-ship`. Second-pass target: `none`. This is a governance recommendation, not an automatic rewrite instruction.

## Cleanup Rationale

- Placeholder top-level actions should not survive handover without an owner and implementation path.
- This workflow is explicitly marked as not yet implemented.

## Handover Notes

Use this page as the human-facing workflow brief during audits, cleanup, and handover. Promote any missing operational knowledge back into the canonical page rather than leaving it in chat.
