---
title: "Update a livestream"
openapi: "PATCH /stream/{id}"
---

<RequestExample>

```bash cURL
curl --request PATCH \
  --url https://livepeer.studio/api/stream/{id} \
  --header 'Authorization: <api-key>' \
  --header 'Content-Type: application/json' \
  --data '{
  "record": true,
}'
```

```javascript JavaScript
import { Livepeer } from "livepeer";

const livepeer = new Livepeer({
  apiKey: "<api-key>",
});

const id = "stream_id";

await livepeer.stream.update(id, {
  record: true,
});
```

```go Go
package main

import(
	"context"
	"log"
	"livepeer"
	"livepeer/models/components"
)

func main() {
    client := livepeer.New(
        livepeer.WithSecurity("<api-key>"),
    )

	var id string = "stream_id"

    ctx := context.Background()
    res, err := client.Stream.Update(ctx, id, components.StreamPatchPayload{
        record: true,
    })

    if err != nil {
        log.Fatal(err)
    }

    if res.Data != nil {
        // handle response
    }
}
```

```python Python
import livepeer
from livepeer.models import components

client = Livepeer.SDK(
    api_key="",
)

req = components.NewStreamPayload(
    name='test_stream',
)

id = 'stream_id'

res = client.stream.update(id=id, stream_patch_payload=components.StreamPatchPayload(
    record=True,
))


if res.data is not None:
    # handle response
    pass
```

```ruby Ruby
require_relative livepeer


client = Livepeer::SDK.new
client.config_security(
  security=Shared::Security.new(
    api_key="",
  )
)


req = Operations::UpdateStreamRequest.new(
  path_params=Operations::UpdateStreamRequest.new(
    id="stream_id",
    stream_patch_payload=Shared::StreamPatchPayload.new(
    record=true,
  ),
	)
)

res = client.stream.update(req)

if !res.data.nil?
  # handle response
end
```

</RequestExample>
