---
title: "Retrieve clips of a session"
openapi: "GET /session/{id}/clips"
---

<RequestExample>

```bash cURL
curl --request GET \
  --url https://livepeer.studio/api/session/{id}/clips \
  --header 'Authorization: Bearer <api-key>'
```

```javascript JavaScript
import { Livepeer } from "livepeer";

const livepeer = new Livepeer({
  apiKey: "<api-key>",
});

const id = "08eeeda4-681f-46e5-a760-63d6cac91d91";

await livepeer.session.getAllClips();
```

```go Go
package main

import (
	"context"
	"log"
	"livepeer"
)

func main() {
	client := livepeer.New(
		livepeer.WithSecurity("<api-key>"),
	)

	ctx := context.Background()

  var id string = "08eeeda4-681f-46e5-a760-63d6cac91d91"
	res, err := client.Session.GetAllClips(ctx, id)

	if err != nil {
		log.Fatal(err)
	}

	if res.Stream != nil {
		// Handle the response (res.Stream)
	}
}
```

```python Python
import livepeer
from livepeer.models import components

client = livepeer.Livepeer(
    api_key="<api-key>"
)

res = client.session.get_all_clips(id='08eeeda4-681f-46e5-a760-63d6cac91d91')


if res.data is not None:
    # Handle the response (res.data)
    pass
```

```ruby Ruby
require_relative 'livepeer'

client = Livepeer::SDK.new
client.config_security(
  security=Shared::Security.new(
    api_key="<api-key>",
  )
)

req = Operations::GetSessionIdClipsRequest.new(
  path_params=Operations::GetSessionIdClipsRequest.new(
    id="08eeeda4-681f-46e5-a760-63d6cac91d91",
  ),
)

res = client.session.get_all_clips(req)


if !res.data.nil?
  # Handle the response (res.data)
end
```

</RequestExample>
