---
title: "Retrieve recorded sessions"
openapi: "GET /stream/{parentId}/sessions"
---

<RequestExample>

```bash cURL
curl --request GET \
  --url https://livepeer.studio/api/session \
  --header 'Authorization: Bearer <api-key>'
```

```javascript JavaScript
import { Livepeer } from "livepeer";

const livepeer = new Livepeer({
  apiKey: "<api-key>",
});

const parentId = "08eeeda4-681f-46e5-a760-63d6cac91d91";
const record = 1; // 0 or 1

await livepeer.session.getRecorded();
```

```go Go
package main

import (
	"context"
	"log"
	"livepeer"
)

func main() {
	client := livepeer.New(
		livepeer.WithSecurity("<api-key>"),
	)

	ctx := context.Background()

  var parentID string = "08eeeda4-681f-46e5-a760-63d6cac91d91"
  var record *int64 = 1 // 0 or 1
	res, err := Session.GetRecorded(ctx, parentID, record)


	if err != nil {
		log.Fatal(err)
	}

	if res.Stream != nil {
		// Handle the response (res.Stream)
	}
}
```

```python Python
import livepeer
from livepeer.models import components

client = livepeer.Livepeer(
    api_key="<api-key>"
)

res = client.session.get_recorded(parent_id='08eeeda4-681f-46e5-a760-63d6cac91d91', record=1)


if res.data is not None:
    # Handle the response (res.data)
    pass
```

```ruby Ruby
require_relative 'livepeer'

client = Livepeer::SDK.new
client.config_security(
  security=Shared::Security.new(
    api_key="<api-key>",
  )
)

req = Operations::GetRecordedSessionsRequest.new(
  path_params=Operations::GetRecordedSessionsRequest.new(
    parent_id="08eeeda4-681f-46e5-a760-63d6cac91d91",
    record=1,
  ),
)

res = s.session.get_recorded(req)

if !res.data.nil?
  # Handle the response (res.data)
end
```

</RequestExample>
