---
title: "Update a signing key"
openapi: "PATCH /access-control/signing-key/{keyId}"
---

<RequestExample>

```bash cURL
curl --request PATCH \
  --url https://livepeer.studio/api/access-control/signing-key/{keyId} \
  --header 'Authorization: <api-key>' \
  --header 'Content-Type: application/json' \
  --data '{
		"disabled": true,
		"name": "<string>",
	}'
```

```javascript JavaScript
import { Livepeer } from "livepeer";

const livepeer = new Livepeer({
  apiKey: "<api-key>",
});

const id = "08eeeda4-681f-46e5-a760-63d6cac91d91";
const requestBody = {
  name: "an updated name",
};

await livepeer.accessControl.updateSigningKey(id, requestBody);
```

```go Go
package main

import (
	"context"
	"log"
	"livepeer"
)

func main() {
	client := livepeer.New(
		livepeer.WithSecurity("<api-key>"),
	)

	ctx := context.Background()
	id := "08eeeda4-681f-46e5-a760-63d6cac91d91"

	requestBody := operations.UpdateSigningKeyRequestBody{
		Name: "an updated name",
	}


	res, err := client.AccessControl.GetSigningKey(ctx, id, requestBody)

	if err != nil {
		log.Fatal(err)
	}

	if res.Stream != nil {
		// Handle the response (res.Stream)
	}
}
```

```python Python
import livepeer
from livepeer.models import components

client = livepeer.Livepeer(
    api_key="<api-key>"
)

id = "08eeeda4-681f-46e5-a760-63d6cac91d91"
request_body = operations.UpdateSigningKeyRequestBody(
		name="an updated name",
)

res = client.access_control.update_signing_key(id=id, request_body=request_body)

if res.data is not None:
    # Handle the response (res.data)
    pass

```

```ruby Ruby
require_relative 'livepeer'

client = Livepeer::SDK.new
client.config_security(
  security=Shared::Security.new(
    api_key="<api-key>",
  )
)

req = Operations::UpdateSigningKeyRequest.new(
  path_params=Operations::UpdateSigningKeyRequest.new(
    id="08eeeda4-681f-46e5-a760-63d6cac91d91",
  ),
	request_body=Operations::UpdateSigningKeyRequestBody.new(
    name="an updated name",
  ),
)



res = client.access_control.update_signing_key(req)

if !res.data.nil?
  # Handle the response (res.data)
end

```

</RequestExample>
