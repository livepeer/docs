---
title: "Create a webhook"
openapi: "POST /webhook"
---

<RequestExample>

```bash cURL
curl --request POST \
  --url https://livepeer.studio/api/webhook \
  --header 'Authorization: Bearer <api-key>' \
  --header 'Content-Type: application/json' \
  --data '{
  	"name": "my first webhook",
		"url": "https://example.com/webhook",
	}'
```

```javascript JavaScript
import { Livepeer } from "livepeer";

const livepeer = new Livepeer({
  apiKey: "<api-key>",
});

await livepeer.webhook.create({
  name: "my first webhook",
  url: "https://example.com/webhook",
});
```

```go Go
package main

import(
	"context"
	"log"
	"livepeer"
	"livepeer/models/components"
)

func main() {
    client := livepeer.New(
        livepeer.WithSecurity("<api-key>"),
    )

    ctx := context.Background()
    res, err := client.Asset.Create(ctx, components.NewWebhookPayload{
        Name: "my first webhook",
				Url: "https://example.com/webhook",
    })

    if err != nil {
        log.Fatal(err)
    }

    if res.Data != nil {
        // handle response
    }
}
```

```python Python
import livepeer
from livepeer.models import components

client = Livepeer.SDK(
    api_key="",
)

req = components.NewWebhookPayload(
    name='my first asset',
		url='https://example.com/webhook',
)

res = client.webhook.create(req)

if res.data is not None:
    # handle response
    pass
```

```ruby Ruby
require_relative livepeer


client = Livepeer::SDK.new
client.config_security(
  security=Shared::Security.new(
    api_key="",
  )
)


req = Shared::NewWebhookPayload.new(
  request=Shared::NewWebhookPayload.new(
    name='my first asset',
		url='https://example.com/webhook',
  ),
)

res = client.webhook.create(req)

if !res.data.nil?
  # handle response
end
```

</RequestExample>
