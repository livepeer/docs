---
title: "Update a webhook"
openapi: "PUT /webhook/{id}"
---

<RequestExample>

```bash cURL
curl --request PUT \
  --url https://livepeer.studio/api/webhook/{id} \
  --header 'Authorization: <api-key>' \
  --header 'Content-Type: application/json' \
```

```javascript JavaScript
import { Livepeer } from "livepeer";

const livepeer = new Livepeer({
  apiKey: "<api-key>",
});

const id = "08eeeda4-681f-46e5-a760-63d6cac91d91";

await livepeer.webhook.update(id, requestBody);
```

```go Go
package main

import (
	"context"
	"log"
	"livepeer"
)

func main() {
	client := livepeer.New(
		livepeer.WithSecurity("<api-key>"),
	)

	ctx := context.Background()
	id := "08eeeda4-681f-46e5-a760-63d6cac91d91"

	res, err := client.webhook.update(ctx, id, requestBody)

	if err != nil {
		log.Fatal(err)
	}

	if res.Stream != nil {
		// Handle the response (res.Stream)
	}
}
```

```python Python
import livepeer
from livepeer.models import components

client = livepeer.Livepeer(
    api_key="<api-key>"
)

id = "08eeeda4-681f-46e5-a760-63d6cac91d91"

res = client.webhook.update(id=id, request_body=request_body)

if res.data is not None:
    # Handle the response (res.data)
    pass

```

```ruby Ruby
require_relative 'livepeer'

client = Livepeer::SDK.new
client.config_security(
  security=Shared::Security.new(
    api_key="<api-key>",
  )
)

req = Operations::UpdateWebhookRequest.new(
  path_params=Operations::UpdateWebhookRequest.new(
    id="08eeeda4-681f-46e5-a760-63d6cac91d91",
  ),
	request_body,
)



res = client.webhook.update(req)

if !res.data.nil?
  # Handle the response (res.data)
end

```

</RequestExample>
