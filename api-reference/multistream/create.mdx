---
title: "Create a multistream"
openapi: "POST /multistream/target"
---

<RequestExample>

```bash cURL
curl --request POST \
  --url https://livepeer.studio/api/multistream/target \
  --header 'Authorization: Bearer <api-key>' \
  --header 'Content-Type: application/json' \
  --data '{
  "name": "my first multistream",
  "url": "rtmps://live.my-service.tv/channel/secretKey",
}'
```

```javascript JavaScript
import { Livepeer } from "livepeer";

const livepeer = new Livepeer({
  apiKey: "<api-key>",
});

await livepeer.multistreamTarget.create({
  name: "my first multistream",
  url: "rtmps://live.my-service.tv/channel/secretKey",
});
```

```go Go
package main

import(
	"context"
	"log"
	"livepeer"
	"livepeer/models/components"
)

func main() {
    client := livepeer.New(
        livepeer.WithSecurity("<api-key>"),
    )

    ctx := context.Background()
    res, err := client.MultistreamTarget.Create(ctx, components.MultistreamTargetInput{
        Name: "my first multistream",
        URL: "rtmps://live.my-service.tv/channel/secretKey",
    })

    if err != nil {
        log.Fatal(err)
    }

    if res.Data != nil {
        // handle response
    }
}
```

```python Python
import livepeer
from livepeer.models import components

client = Livepeer.SDK(
    api_key="",
)

req = components.MultistreamTargetInput(
    name='my first multistream',
		url='rtmps://live.my-service.tv/channel/secretKey',
)

res = client.multistream_target.create(req)

if res.data is not None:
    # handle response
    pass
```

```ruby Ruby
require_relative livepeer


client = Livepeer::SDK.new
client.config_security(
  security=Shared::Security.new(
    api_key="",
  )
)


req = Shared::MultistreamTargetInput.new(
  request=Shared::MultistreamTargetInput.new(
    name="my first multistream",
		url="rtmps://live.my-service.tv/channel/secretKey",
  ),
)

res = client.multistream_target.create(req)

if !res.data.nil?
  # handle response
end
```

</RequestExample>
