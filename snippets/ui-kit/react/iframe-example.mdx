export const ExampleWithCode = ({ type, id }) => (
  <iframe
    src={`https://ui-kit-docs-embed.vercel.app/${type}/${id}`}
    height={600}
    width="100%"
    frameborder="0"
    allowfullscreen
    allow="autoplay; encrypted-media; fullscreen; picture-in-picture; display-capture; camera; microphone"
    style={{
      borderRadius: 10,
      backgroundColor: "black",
    }}
  />
);
