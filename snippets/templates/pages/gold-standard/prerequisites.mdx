{/*
SECTION BLOCK: Prerequisites
PURPOSE: What the reader needs before starting.

WHEN TO USE:
- Required on instruction and tutorial pages
- Optional on guide pages (when the guide includes procedural steps)

WHEN NOT TO USE:
- concept, reference, navigation, resource pages
- Pages with no procedural content

RULES:
- Always placed BEFORE any step sequence
- Never inside steps or Accordions
- If a prerequisite links to another page, the link must work
- If a prerequisite is optional, mark it clearly
*/}

{/* ─── VARIANT 1: Simple list (tutorials, fewer than 5 prerequisites) ─── */}

{/*
## Prerequisites

- **[Prerequisite 1]** — [why it's needed]. [See [setup guide](/v2/path/to/guide).]
- **[Prerequisite 2]** — [why it's needed].
- **[Prerequisite 3]** — [why it's needed].
*/}

{/* ─── VARIANT 2: StyledTable (instruction/setup, 3+ prerequisites) ─── */}

{/*
import { StyledTable, TableRow, TableCell } from '/snippets/components/wrappers/tables/Tables.jsx'
import { CustomDivider } from '/snippets/components/elements/spacing/Divider.jsx'

## Prerequisites

<StyledTable>
  <TableRow header>
    <TableCell>Requirement</TableCell>
    <TableCell>Why</TableCell>
    <TableCell>How to Get It</TableCell>
  </TableRow>
  <TableRow>
    <TableCell>[Requirement 1 — e.g., Docker 24+]</TableCell>
    <TableCell>[Brief reason — e.g., Container runtime for node deployment]</TableCell>
    <TableCell>[Link or instruction — e.g., [Install Docker](https://docs.docker.com)]</TableCell>
  </TableRow>
  <TableRow>
    <TableCell>[Requirement 2]</TableCell>
    <TableCell>[Brief reason]</TableCell>
    <TableCell>[Link or instruction]</TableCell>
  </TableRow>
</StyledTable>

<CustomDivider />
*/}
