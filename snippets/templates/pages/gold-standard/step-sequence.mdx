{/*
SECTION BLOCK: Step Sequence
PURPOSE: Ordered actions to accomplish a task.

WHEN TO USE:
- Primary on instruction and tutorial pages
- Sometimes in guide pages (when guide includes procedural sections)

WHEN NOT TO USE:
- concept, reference, navigation, resource pages
- Never put steps inside Accordions (Accordions are supplementary)

RULES:
- One action per step — two actions = two steps
- Every step has a visible result (what the reader should see)
- Action >= explanation — if 3 lines why / 1 line what, restructure
- Use Accordion INSIDE a step for optional depth (not the reverse)
- Code blocks have filename, language, and icon props
*/}

{/*
import { StyledSteps, StyledStep } from '/snippets/components/wrappers/steps/StyledSteps.jsx'
import { CustomCodeBlock } from '/snippets/components/displays/code/CustomCodeBlock.jsx'
import { CustomDivider } from '/snippets/components/elements/spacing/Divider.jsx'

## [Section Heading — governing-concept label, 1-3 words]

<StyledSteps>
  <StyledStep title="[Action Verb + Object]">
    [1-2 sentences explaining what this step does and why.]

    <CustomCodeBlock language="bash" icon="terminal" title="terminal">
    {`[command to run]`}
    </CustomCodeBlock>

    Expected result: [What the reader should see after running this command.]

    <Accordion title="[Optional detail heading]" icon="circle-info">
      [Extended explanation, alternative approaches, or background context.
      This is supplementary — the step works without opening this.]
    </Accordion>
  </StyledStep>

  <StyledStep title="[Action Verb + Object]">
    [1-2 sentences explaining what this step does.]

    <CustomCodeBlock language="bash" icon="terminal" title="terminal">
    {`[command to run]`}
    </CustomCodeBlock>

    Expected result: [What the reader should see.]
  </StyledStep>

  <StyledStep title="[Action Verb + Object]">
    [1-2 sentences explaining what this step does.]

    <CustomCodeBlock language="bash" icon="terminal" title="terminal">
    {`[command to run]`}
    </CustomCodeBlock>

    Expected result: [What the reader should see.]
  </StyledStep>
</StyledSteps>

<CustomDivider />
*/}
