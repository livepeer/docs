{/*
SECTION BLOCK: Verification Checklist
PURPOSE: Confirm something works after setup, configuration, or build.

WHEN TO USE:
- Required on instruction (setup) pages
- Recommended on tutorial pages (verify the built artefact)
- Sometimes on guide pages (verify a configuration change)

WHEN NOT TO USE:
- concept, reference, navigation, resource pages
- Pages with no procedural content

RULES:
- Every check has an expected output (what the reader should see)
- Commands are copy-able (use CustomCodeBlock with filename/language/icon)
- Include both positive checks (it works) and negative checks (common failures)
- If a check fails, point to troubleshooting or the relevant fix
*/}

{/*
import { StyledSteps, StyledStep } from '/snippets/components/wrappers/steps/StyledSteps.jsx'
import { CustomCodeBlock } from '/snippets/components/displays/code/CustomCodeBlock.jsx'
import { CustomDivider } from '/snippets/components/elements/spacing/Divider.jsx'

## Verification

<StyledSteps>
  <StyledStep title="[Check 1 — e.g., Confirm Service Running]">
    [One sentence explaining what this check validates.]

    <CustomCodeBlock language="bash" icon="terminal" title="terminal">
    {`[command to run — e.g., docker ps | grep livepeer]`}
    </CustomCodeBlock>

    **Expected result**: [What the reader should see — e.g., "A running container named
    `livepeer-gateway` with status `Up`."]

    <Warning>
      If you see [common failure], check [specific cause]. See [troubleshooting link].
    </Warning>
  </StyledStep>

  <StyledStep title="[Check 2 — e.g., Test Endpoint]">
    [One sentence explaining what this check validates.]

    <CustomCodeBlock language="bash" icon="terminal" title="terminal">
    {`[command — e.g., curl http://localhost:8935/status]`}
    </CustomCodeBlock>

    **Expected result**: [What the reader should see — e.g., "JSON response with
    `"Manifests":{}` and node version."]
  </StyledStep>

  <StyledStep title="[Check 3 — e.g., Verify On-chain Registration]">
    [One sentence explaining what this check validates.]

    <CustomCodeBlock language="bash" icon="terminal" title="terminal">
    {`[command — e.g., livepeer_cli -host 127.0.0.1 -http 7935]`}
    </CustomCodeBlock>

    **Expected result**: [What the reader should see — e.g., "Node stats showing
    registered orchestrator address and current stake."]
  </StyledStep>
</StyledSteps>

<CustomDivider />
*/}
