{/*
SECTION BLOCK: Comparison Table
PURPOSE: Side-by-side evaluation of options, types, or approaches.

WHEN TO USE:
- concept pages (deployment types, workload types, node roles)
- guide pages (setup options, pricing models)
- reference pages (feature comparison)

WHEN NOT TO USE:
- tutorial, instruction (these execute, not compare)
- navigation (pure routing)
- When there are only 2 options with 1-2 attributes (use prose or CardGroup instead)

RULES:
- Opening definition paragraph BEFORE the table
- Equal visual weight — no option gets more space than another
- No "vs" in headings — use governing-concept labels
- Columns: Axis | Options | What It Decides (or similar structured layout)
*/}

{/*
import { StyledTable, TableRow, TableCell } from '/snippets/components/wrappers/tables/Tables.jsx'
import { CustomDivider } from '/snippets/components/elements/spacing/Divider.jsx'

## [Governing-Concept Label — e.g., "Deployment Types", "Workload Comparison"]

[Opening paragraph defining the comparison. What is the reader evaluating?
What are the axes of comparison? Frame the decision before showing the data.

E.g., "Livepeer supports three deployment types, each with different operational
complexity, hardware requirements, and earning potential."]

<StyledTable variant="bordered">
  <TableRow header>
    <TableCell>Axis</TableCell>
    <TableCell>Options</TableCell>
    <TableCell>What It Decides</TableCell>
  </TableRow>
  <TableRow>
    <TableCell>[Axis 1 — e.g., Deployment Mode]</TableCell>
    <TableCell>[Options — e.g., Docker, Binary, Pool Worker]</TableCell>
    <TableCell>[Impact — e.g., Operational complexity and update workflow]</TableCell>
  </TableRow>
  <TableRow>
    <TableCell>[Axis 2 — e.g., Network Mode]</TableCell>
    <TableCell>[Options — e.g., Off-chain, On-chain]</TableCell>
    <TableCell>[Impact — e.g., Whether payments settle on the blockchain]</TableCell>
  </TableRow>
  <TableRow>
    <TableCell>[Axis 3 — e.g., Workload Type]</TableCell>
    <TableCell>[Options — e.g., Video, AI, Dual]</TableCell>
    <TableCell>[Impact — e.g., Hardware requirements and earning streams]</TableCell>
  </TableRow>
</StyledTable>

<CustomDivider />
*/}
