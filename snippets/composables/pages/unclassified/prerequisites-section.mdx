{/*
  @composable  prerequisites-section
  @purpose     Upfront requirements list — concrete setup steps and access the reader must have before starting.
  @pageTypes   instruction (how-to), tutorial, setup, start
  @variables
    - Bullet items: one requirement per line; link to the setup or docs page for each dependency
    - Inline note:  brief clarification after the em dash — omit if self-evident
  @notes       Keep to 3–6 items. If more are needed, group into sub-lists with bold headers (see optional block).
               List only concrete prerequisites: tools, accounts, tokens, environment variables.
               Do NOT list knowledge prerequisites — only setup steps and access requirements.
               Pair with steps-section.mdx as the next section.
  @promoted    2026-03-21
*/}

## Prerequisites

- [Dependency or access requirement](/path-to-setup) — brief note if context is needed.
- [Second requirement](/path-to-docs) — brief note if context is needed.
- Required environment variable or account permission (no link needed if self-evident).

{/*
  OPTIONAL — grouped prerequisites for multi-environment or multi-role pages:

  **Environment setup**
  - [Node.js 18+](https://nodejs.org) — runtime for the scripts used in this guide.
  - [Livepeer Studio account](/v2/developers/quickstart) — API key required in the next step.

  **Access**
  - Operator wallet with ETH for gas fees.
  - Orchestrator node running Livepeer v3.x or later.
*/}
