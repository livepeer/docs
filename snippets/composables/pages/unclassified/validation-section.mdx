{/*
  @composable  validation-section
  @purpose     Post-task validation block — confirms the preceding steps succeeded with observable checks.
  @pageTypes   instruction (how-to), tutorial, start
  @variables
    - Check items:     one observable output or state per bullet — must be falsifiable
    - Optional command: the verification command (curl, health endpoint, CLI status call)
  @notes       Every check item must be falsifiable — "it works" or "you should see it running" are not valid.
               Describe the exact output, response code, UI state, or log line that confirms success.
               Place this section immediately after steps-section.mdx and before related-resources-section.mdx.
  @promoted    2026-03-21
*/}

## Validate

- Expected output, status code, or UI state that confirms the task succeeded.
- Second confirmation signal — only include if genuinely independent from the first check.

{/*
  OPTIONAL — verification command:

  ```bash title="Terminal" icon="terminal"
  # Command that returns a verifiable success response
  curl -s https://example.livepeer.test/health | jq '.status'
  # Expected output: "ok"
  ```
*/}
