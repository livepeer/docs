{/* API Base URLs Table Snippet
    Usage: Include this in API landing pages for styled base URL tables
    Pass props: urls (array of {name, url} objects) */}

export const ApiBaseUrlsTable = ({ urls }) => (
  <div style={{ overflowX: "auto", marginBottom: "24px" }}>
    <table
      style={{ width: "100%", borderCollapse: "collapse", fontSize: "0.9rem" }}
    >
      <thead>
        <tr style={{ backgroundColor: "#2d9a67", color: "#fff" }}>
          <th
            style={{
              padding: "12px 16px",
              textAlign: "left",
              fontWeight: "600",
              borderBottom: "2px solid #2d9a67",
            }}
          >
            Environment
          </th>
          <th
            style={{
              padding: "12px 16px",
              textAlign: "left",
              fontWeight: "600",
              borderBottom: "2px solid #2d9a67",
            }}
          >
            URL
          </th>
        </tr>
      </thead>
      <tbody>
        {urls.map((item, index) => (
          <tr
            key={index}
            style={{
              borderBottom: "1px solid #333",
              backgroundColor: index % 2 === 0 ? "#1a1a1a" : "transparent",
            }}
          >
            <td
              style={{
                padding: "10px 16px",
                color: "#2d9a67",
                fontWeight: "500",
              }}
            >
              {item.name}
            </td>
            <td style={{ padding: "10px 16px", fontFamily: "monospace" }}>
              <code>{item.url}</code>
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
);
