export const TabsContainer = ({ children }) => (
  <div style={{ border: "1px solid var(--accent)", borderRadius: "12px", padding: "1rem" }}>
    <Tabs>
      {children}
    </Tabs>
  </div>
);

export const CardContentContainer = ({ children, align, justify, gap = "1rem" }) => (
  <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
    {children}
  </div>
);

export const CalloutContainer = ({ children, width="fit-content", align="center" }) => (
  <div style={{ display: "flex", flexDirection: "column", gap: "1rem", width: width, align: align }}>
    {children}
  </div>
);
