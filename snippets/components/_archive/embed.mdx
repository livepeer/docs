export const PdfEmbed = ({
  title,
  src,
  height = "700px",
  width = "100%",
}) => (
  <Frame caption={title}>
    <iframe src={src} width={width} height={height} frameborder="0" title={title}></iframe>
  </Frame>
);
