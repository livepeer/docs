# 02.5 - Research Pack Mapping: Orchestrators Tab

**Date**: 2026-03-23
**Source**: orchestrators-research-pack.md mapped against canonical IA sections S1-S12

---

## S1 - Which path is mine? (Disambiguation)

### Existing pages with content for this section
- `v2/orchestrators/navigator.mdx` -- Decision tree navigator by operator situation. Strong fit.
- `v2/orchestrators/quickstart/dep-x-setup-paths.mdx` -- Path selection table (pool/solo/pool-operator/enterprise). Overlaps with navigator.
- `v2/orchestrators/guides/deployment-details/setup-options.mdx` -- Deployment configuration overview (on-chain, off-chain, siphon, pool, solo, dual). Overlaps.

### Content gaps
- No single unified disambiguation page that routes by hardware + capital + goals (the three-variable decision the IA requires).
- Three overlapping pages serve this function partially but none has the complete decision framework.
- Missing: explicit routing for AI-only operators who do not need LPT stake.

### Critical flags
- `dep-x-setup-paths.mdx` references paths that may not exist (`/v2/orchestrators/guides/join-a-pool`).
- `operator-rationale.mdx` has stray characters `glrw\npwrfs` before YAML frontmatter (formatting issue).

---

## S2 - Is this viable for my hardware and situation?

### Existing pages
- `v2/orchestrators/guides/operator-considerations/operator-rationale.mdx` -- Cost categories, revenue streams, viability thresholds, decision matrix. Strong fit.
- `v2/orchestrators/guides/operator-considerations/business-case.mdx` -- Commercial focus: service fees, gateway relationships. Partial fit (commercial variant).
- `v2/orchestrators/concepts/role.mdx` -- Role history, deployment types, operator personas. Partial fit.
- `v2/orchestrators/concepts/incentive-model.mdx` -- Revenue streams, pricing, cost structure. Partial fit.

### Content gaps
- No single page frames the go/no-go decision from the hardware-first perspective the IA requires.
- The delegator-turned-operator comparison (solo operator earnings vs continued delegation) is not explicitly framed anywhere.
- Missing: explicit comparison of earnings by path (solo video vs AI-only vs dual vs pool).

### Critical flags
- `operator-rationale.mdx` frontmatter corruption (stray characters).

---

## S3 - How does work reach me and how do I get paid?

### Existing pages
- `v2/orchestrators/concepts/capabilities.mdx` -- Capability advertisement, gateway selection. Good fit.
- `v2/orchestrators/concepts/architecture.mdx` -- Request flow, protocol interactions. Partial fit.
- `v2/orchestrators/concepts/incentive-model.mdx` -- Two revenue streams, payment flow. Good fit.
- `v2/orchestrators/guides/staking-and-rewards/earning-model.mdx` -- ETH fees + LPT rewards, PM tickets. Good fit.
- `v2/orchestrators/guides/staking-and-rewards/reward-mechanics.mdx` -- Reward calling, LIP-89, inflation. Good fit.
- `v2/orchestrators/guides/payments-and-pricing/payments.mdx` -- PM mechanics, Redeemer, redemption. Good fit.
- `v2/orchestrators/guides/payments-and-pricing/payment-receipts.mdx` -- Ticket verification. Partial fit.
- `v2/orchestrators/guides/ai-and-job-workloads/workload-options.mdx` -- Four job types with routing differences. Good fit.
- `v2/orchestrators/guides/advanced-operations/gateway-relationships.mdx` -- Gateway discovery and selection. Good fit.
- `v2/orchestrators/guides/advanced-operations/gateway-orchestrator-interface.mdx` -- gRPC interface. Partial fit.

### Content gaps
- Content is fragmented across 10+ pages. S3 needs a single cohesive explanation of routing + payment.
- Missing: explicit statement that active set membership is NOT required for AI inference routing (flagged in IA gap notes).
- The distinction between stake-based routing (video) and capability-based routing (AI) exists across multiple pages but is not presented as a single clear comparison.

### Critical flags
- None in existing content.

---

## S4 - Prerequisites: what must be in place before setup

### Existing pages
- `v2/orchestrators/setup/prepare.mdx` -- Pre-flight checklist by node mode. Strong fit.
- `v2/orchestrators/setup/connect-and-activate.mdx` -- Wallet funding, LPT staking, RPC setup. Partial fit (mixes prerequisites with setup).
- `v2/orchestrators/guides/operator-considerations/requirements.mdx` -- GPU support, NVENC limits, VRAM, system minimums. Strong fit.
- `v2/orchestrators/resources/gpu-support.mdx` -- GPU compatibility matrix. Reference.
- `v2/orchestrators/resources/arbitrum-rpc.mdx` -- RPC provider comparison. Reference.
- `v2/orchestrators/resources/arbitrum-exchanges.mdx` -- Where to acquire ETH and LPT. Reference.
- `v2/orchestrators/guides/config-and-optimisation/capacity-planning.mdx` -- maxSessions, benchmarking. Partial fit.

### Content gaps
- LPT acquisition path is not clearly separated as "solo video path only" (IA gap note 2).
- Pool node operators' prerequisite exemption from LPT is not explicit (IA gap note 3).
- AI prerequisites vs video prerequisites not cleanly distinguished.
- ETH bridging to Arbitrum is covered in exchanges page but not integrated into prerequisite flow.

### Critical flags
- Research pack notes: v1 hardware-requirements had incomplete RAM/Disk sections (TBD). Verify v2 fills this.
- Research pack notes: v1 benchmark-transcoding details (ABR ladder, livepeer_bench loop) may not be in v2 capacity-planning.

---

## S5 - Get your node running

### Existing pages
- `v2/orchestrators/setup/rs-install.mdx` -- Docker and binary install. Strong fit.
- `v2/orchestrators/setup/configure.mdx` -- docker-compose templates for three modes. Strong fit.
- `v2/orchestrators/setup/connect-and-activate.mdx` -- On-chain activation, service URI. Strong fit.
- `v2/orchestrators/setup/guide.mdx` -- Setup flow overview. Strong fit.
- `v2/orchestrators/guides/deployment-details/orchestrator-transcoder-setup.mdx` -- O-T split. Variant.
- `v2/orchestrators/guides/deployment-details/dual-mode-configuration.mdx` -- Dual mode setup. Variant.
- `v2/orchestrators/guides/deployment-details/siphon-setup.mdx` -- Siphon setup. Variant.
- `v2/orchestrators/guides/ai-and-job-workloads/video-transcoding-operations.mdx` -- Video operations. Partial fit.

### Content gaps
- Multi-platform binary install details (Windows, AUR package, source build with BUILD_TAGS) from v1 are less prominent in v2.
- S5 design flag: may need to split solo setup from pool node setup.

### Critical flags
- `s-guide.mdx` appears to be a deprecated duplicate setup overview with broken internal links.
- Research pack: v1 install has explicit release URL patterns, v2 uses Docker primarily.

---

## S6 - Join as a pool node

### Existing pages
- `v2/orchestrators/guides/deployment-details/join-a-pool.mdx` -- Pool vs solo comparison, steps to join, Titan Node reference. Strong fit.
- `v2/orchestrators/guides/deployment-details/new-join-a-pool.mdx` -- Updated pool joining guide. Appears to be a revision.
- `v2/orchestrators/resources/community-pools.mdx` -- Pool listing page. Reference.
- `v2/orchestrators/guides/advanced-operations/pool-operators.mdx` -- Running a pool (operator side). Adjacent.

### Content gaps
- Two versions of join-a-pool exist (join-a-pool.mdx and new-join-a-pool.mdx). Need consolidation decision.
- The "enough stake?" decision with sufficient context for pool vs solo vs AI-first is not fully resolved in either page.
- Missing: clear statement that pool node operators do not need LPT (present in glossary but not prominent in pool guide).

### Critical flags
- No canonical pool directory exists (noted in existing content). Discovery is off-chain/social.

---

## S7 - Set your pricing and cuts

### Existing pages
- `v2/orchestrators/guides/config-and-optimisation/pricing-strategy.mdx` -- Video and AI pricing, per-gateway, autoAdjust. Strong fit (but draft status).
- `v2/orchestrators/guides/staking-and-rewards/delegate-operations.mdx` -- Commission strategy, delegator attractiveness. Partial fit.
- `v2/orchestrators/concepts/incentive-model.mdx` -- Fee/reward cut mechanics. Partial fit.

### Content gaps
- Pricing strategy page is in draft status.
- Cut settings must be framed from operator's perspective (what to set and why), not delegator's (IA gap note 4).
- Missing: benchmark pricing ranges for video and AI that operators can use as starting points.

### Critical flags
- Research pack: v1 fiat pricing (USD via Chainlink) should be verified as still current.

---

## S8 - Set up AI pipelines

### Existing pages
- `v2/orchestrators/guides/ai-and-job-workloads/diffusion-pipeline-setup.mdx` -- Diffusion pipeline. Strong fit.
- `v2/orchestrators/guides/ai-and-job-workloads/llm-pipeline-setup.mdx` -- LLM pipeline. Strong fit.
- `v2/orchestrators/guides/ai-and-job-workloads/realtime-ai-setup.mdx` -- Cascade/live AI. Strong fit.
- `v2/orchestrators/guides/ai-and-job-workloads/audio-and-vision-pipelines.mdx` -- Audio, vision pipelines. Strong fit.
- `v2/orchestrators/guides/ai-and-job-workloads/ai-inference-operations.mdx` -- AI operations. Strong fit.
- `v2/orchestrators/guides/ai-and-job-workloads/model-hosting.mdx` -- Model hosting, VRAM. Strong fit.
- `v2/orchestrators/guides/ai-and-job-workloads/model-demand-reference.mdx` -- Model demand. Reference.
- `v2/orchestrators/guides/config-and-optimisation/ai-model-management.mdx` -- Warm/cold models, VRAM allocation. Strong fit.
- `v2/orchestrators/quickstart/AI-prompt-start.mdx` -- Quick AI add. Partial fit.
- Tutorials: ai-earning-quickstart, add-ai-to-video-node, full-ai-pipeline-tutorial, realtime-ai-tutorial. Guided paths.

### Content gaps
- BYOC path not covered as a dedicated sub-section (design flag from IA).
- Capability advertisement mechanics (how to verify gateways will route AI work) needs explicit coverage before verification section.
- LIP-92 conflict BLOCKING: cannot cite specific LIP number for AI capability registry.

### Critical flags
- Research pack: aiModels.json schema changes with go-livepeer releases -- high staleness risk.
- All AI configuration flags require verification against latest tagged release.
- S8 design flag: may need to split due to AI configuration surface length.

---

## S9 - Verify your node is working end to end

### Existing pages
- `v2/orchestrators/setup/test.mdx` -- Verification checklist: on-chain, transcoding, AI, reward calling, metrics. Strong fit.

### Content gaps
- Performance score verification is not explicitly covered (how to check and what thresholds mean).
- Pricing gate verification (confirming price is within gateway MaxPrice thresholds) needs explicit step.
- AI capability advertisement verification needs explicit step.

### Critical flags
- None found.

---

## S10 - Monitor and operate day-to-day

### Existing pages
- `v2/orchestrators/guides/monitoring-and-tooling/metrics-and-alerting.mdx` -- Prometheus, Grafana, Docker monitoring. Strong fit.
- `v2/orchestrators/guides/monitoring-and-tooling/explorer-operations.mdx` -- Explorer metrics walkthrough. Strong fit.
- `v2/orchestrators/guides/monitoring-and-tooling/operator-toolbox.mdx` -- Tools index. Strong fit.
- `v2/orchestrators/setup/r-monitor.mdx` -- Post-activation monitoring. Partial fit.
- `v2/orchestrators/guides/staking-and-rewards/reward-mechanics.mdx` -- Reward calling cadence. Partial fit.
- `v2/orchestrators/guides/config-and-optimisation/reward-call-tuning.mdx` -- Gas optimisation for reward calls. Strong fit.

### Content gaps
- No single "day-to-day operations checklist" that consolidates routine tasks and their cadence.
- Ticket redemption monitoring (when to batch, gas cost tracking) is split across payments pages.

### Critical flags
- Research pack: v1 monitor-metrics covered Docker monitoring container; verify v2 equivalent depth.

---

## S11 - Optimise earnings and performance

### Existing pages
- `v2/orchestrators/guides/config-and-optimisation/pricing-strategy.mdx` -- Pricing optimisation. Strong fit.
- `v2/orchestrators/guides/config-and-optimisation/capacity-planning.mdx` -- Session/bandwidth optimisation. Strong fit.
- `v2/orchestrators/guides/config-and-optimisation/ai-model-management.mdx` -- Model warmth, VRAM allocation. Strong fit.
- `v2/orchestrators/guides/staking-and-rewards/delegate-operations.mdx` -- Delegator attractiveness. Strong fit.
- `v2/orchestrators/guides/advanced-operations/gateway-relationships.mdx` -- Gateway selection factors. Partial fit.
- `v2/orchestrators/guides/advanced-operations/scale-operations.mdx` -- Fleet scaling. Partial fit.
- `v2/orchestrators/guides/operator-considerations/business-case.mdx` -- Commercial positioning. Partial fit.

### Content gaps
- No single page that identifies the "most impactful lever" for the reader's specific situation and guides targeted adjustment.
- Performance score improvement guidance is scattered.
- VRAM allocation optimisation (which models to keep warm based on demand) exists in ai-model-management but needs stronger connection to earnings impact.

### Critical flags
- None found.

---

## S12 - Diagnose and fix problems

### Existing pages
- `v2/orchestrators/guides/monitoring-and-tooling/troubleshooting.mdx` -- Categorised errors. Strong fit.
- `v2/orchestrators/resources/faq.mdx` -- Common errors and FAQs, symptom-indexed. Strong fit.

### Content gaps
- IA gap note 5: Troubleshooting must structure as three distinct diagnostic paths: (1) low work volume, (2) operational errors (reward calls, ticket redemptions), (3) performance score degradation. Current pages categorise by error type rather than by symptom-first diagnostic path.
- Two overlapping troubleshooting pages (guides/troubleshooting.mdx and resources/faq.mdx) need consolidation decision.
- `resources/technical/x-troubleshooting.mdx` is a stub that duplicates the concept.

### Critical flags
- None in existing content beyond the structural issue.
