---
title: CONTENTI-PIPLEINE — Master Status
lastAudited: 2026-03-24
---

## Purpose

A complete content production pipeline specifically for the Orchestrators tab. Covers all phases from IA verification through content writing for 12 sections (S01-S12). The folder name typo ("PIPLEINE") is intentional. This pipeline ran independently of the formal CONTENT-WRITING gate sequence and produced the 12 Orchestrators section pages currently awaiting Phase 7 (veracity pass).

## Verified Done

**All pipeline phases 1–6 complete** (per `00-TRACKER.md`, verified against output files):

| Phase | Output file | Verified |
|---|---|---|
| 1 — IA verification | `01-ia-verification.md` | Yes — all 12 sections pass all required field checks; entry/exit chain verified gap-free |
| 1b — Persona check | `01b-persona-check.md` | Yes — output file exists |
| 2 — Content scan | `02-content-scan.md` | Yes — 88 live pages scanned, 68 mapped to IA, 20 unmapped |
| 2.5 — Research pack | `02.5-research-pack.md` | Yes — all 12 sections mapped with gaps and flags |
| 3 — Structure audit | `03-tab-map.md` | Yes — 1 P0, 7 P1, 3 P2 issues; 18 orphan pages |
| 3.5 — Terminology lock | `03.5-terminology-lock.md` | Yes — 30 terms with definitions, verify flags, glossary conflict notes |
| 4 — Content audit | `04-content-audit.md` | Yes — 42 KEEP, 8 REWRITE, 9 MERGE, 2 MOVE, 1 SPLIT, 8 DROP |
| 4.5 — Reconsolidation plan | `04.5-reconsolidation.md` | Yes — 2 file moves, 8 merges, 16 drops defined with rationale |
| 6 — Content writing | `content/S01-S12.mdx` | Yes — all 12 files verified with real content (see below) |

**All 12 content pages — verified with real, substantive content** (spot-checks of S01, S06, S12):
- S01 (`which-path-is-mine`): Full navigation page with path comparison table, LPT question analysis, Card group navigation — proper MDX with components
- S06 (`join-as-pool-node`): Full guide with StyledTable, decision framework, StyledSteps — proper MDX with components
- S12 (`diagnose-and-fix`): Full troubleshooting guide with 3 diagnostic paths, StyledTable, symptom routing — proper MDX with components
- S05 (`get-your-node-running`): Full instruction page with Docker install steps, code blocks — proper MDX with components
- All 12 pages have: valid frontmatter, MDX component imports, real body content, and `VERIFY` or `{/* REVIEW */}` markers on unverified claims

**`03.5-terminology-lock.md`**: 30 terms locked with verify flags, glossary conflict notes for each term. One confirmed conflict identified and documented (Pool node vs Pool worker). Count discrepancy noted (header says 49, table has 30).

**`04.5-reconsolidation.md`**: Specific file moves, merges, and drops with rationale. Executable plan.

## Claimed Done — Unverified or Incomplete

**Phase 7 (veracity pass)**: `00-TRACKER.md` correctly marks this as PENDING. Veracity markers exist within the S01-S12 pages (confirmed in spot-checks — `{/* VERIFY: ... */}` inline) but the systematic veracity pass using `veracity-pass.md` has not run. The CLAUDE.md session log confirms: "Active priority: Orchestrators veracity pass on S01-S12. Phases 1-6 complete. Phase 7 is the current work."

**Gate compliance**: This pipeline ran to completion without the formal IA lock being recorded in `decisions/decision-registry.md` (the authoritative CONTENT-WRITING decisions file). `01-ia-verification.md` verifies the IA structure is sound, but verification within this pipeline is not the same as the formal human sign-off and registry entry required by the CONTENT-WRITING gate protocol. The official `tab-status.md` still shows Orchestrators IA Approved as ⬜.

**Terminology lock vs formal lock**: `03.5-terminology-lock.md` exists and is thorough. However, the CONTENT-WRITING pipeline requires terminology lock to be recorded in `decision-registry.md` with named decision authority. This file is a working document within CONTENTI-PIPLEINE, not a formally registered lock.

**Reconsolidation plan approval**: `04.5-reconsolidation.md` specifies file moves, merges, and drops. Per the CONTENT-WRITING protocol (Step 15), reconsolidation requires explicit human sign-off on every move before execution. No evidence of this approval exists. The plan is written but not approved for execution.

**`content/` pages have MDX component imports**: The pages import from `/snippets/components/...` paths. These pages have not been tested in a Mintlify build environment. Layout validity is assumed, not verified.

## In Progress

**Phase 7 — Veracity pass**: This is the active priority per CLAUDE.md. The `veracity-pass.md` prompt in CONTENT-WRITING is drafted but not yet approved for production. The immediate next step is to test the veracity-pass.md prompt against one or two S01-S12 pages and get human approval before running at scale.

## Not Started / Still To Do

**Phase 7 (veracity pass)**: Pending. ~48+ REVIEW/VERIFY markers across 12 pages need resolution against veracity library sources. `veracityStatus` frontmatter field needs to be set to verified/partial/unverified on each page.

**Phase 8 (naming audit)**: Not started. All section headings in S01-S12 need scoring against the naming rubric (minimum 20/25). Must run before veracity pass per pipeline ordering.

**Phase 10 (layout pass)**: Not started. Pages use MDX components but have not been through the formal layout pass prompt.

**Reconsolidation execution**: `04.5-reconsolidation.md` is a plan, not executed changes. The file moves, merges, and drops in `v2/orchestrators/` described in the plan have not been made.

**Formal gate recording**: IA lock, terminology lock, and reconsolidation approval need to be recorded in CONTENT-WRITING's `decisions/decision-registry.md` to be officially valid.

**Phase 11 (universal pages)**: Not started. Portal, navigator, FAQ, glossary.

**Human review of all 12 pages**: No individual page has been human-reviewed and approved for publication.

## Blockers

| Blocker | Blocks | Owner |
|---|---|---|
| veracity-pass.md prompt test + approval | Phase 7 execution | Alison |
| Naming audit execution (Phase 8 before Phase 7) | Phase 7 | Ready to run — no dependency |
| Formal IA lock in decision-registry.md | Official pipeline gate compliance | Alison |
| Formal reconsolidation plan approval | Execution of file moves in v2/orchestrators/ | Alison |
| LIP-92 verify flag | S01 VERIFY marker resolution | Non-blocking (term definition already locked) |
| Active set threshold live value | S01, S02 VERIFY markers | explorer.livepeer.org (live link, not hardcoded) |
| Fee cut direction convention | S07 and related sections | SME verification against BondingManager contract |

## File Inventory Flags

**Folder name typo**: "CONTENTI-PIPLEINE" — confirmed intentional per task brief.

**No REVIEW-phase output files yet**: The tracker shows Phase 7 as PENDING and nothing beyond. Files `07-veracity-pass.md` and beyond do not exist — this is expected given current state.

**`content/` files use `.mdx` extension but are stored in a workspace planning folder**: These files exist at `workspace/plan/active/CONTENTI-PIPLEINE/content/` but reference absolute snippet import paths (`/snippets/components/...`) as if they will live in the `v2/` tree. The import paths will work correctly only when the files are placed in the `v2/` directory structure. Until then, they cannot be rendered locally via `npx mintlify dev`.

**`00-TRACKER.md` is the only navigation document**: There is no README, index, or higher-level document explaining this pipeline's relationship to the main CONTENT-WRITING plan or ORCHS. New readers must infer the context from `00-TRACKER.md` alone.

**21 files total**: The 8 phase documents, 1 tracker, and 12 content files are all present. No orphaned or stale files detected.

## Audit Notes

CONTENTI-PIPLEINE is the most execution-complete plan for Orchestrators content. Phases 1–6 are genuinely done: the output files contain real artefacts, the content pages contain real MDX with proper structure and voice compliance, and the phase chain is logically consistent (IA → personas → scan → structure → terminology → audit → reconsolidation → content). This is not a plan with stubs or placeholder outputs.

The critical gap is formal gate compliance. This pipeline ran efficiently inside its own folder but outside the formal CONTENT-WRITING gate sequence. The outputs are good but they are not yet officially sanctioned by the CONTENT-WRITING protocol's gate requirements (formal IA lock in registry, formal reconsolidation approval, etc.). The work done here needs to be "landed" into the official pipeline by recording the decisions formally.

Phase 7 is the active work. The veracity-pass.md prompt is the gating dependency — it needs one or two test runs against S01-S12 pages for Alison to approve before running at scale. Once approved, the 12 pages can move through veracity pass relatively quickly.

The reconsolidation plan (`04.5`) is a clean, specific list of file operations for `v2/orchestrators/`. Once formally approved by Alison, those changes can be executed to clean up the live orchestrators tab structure.
