---
title: ORCHS — Master Status
lastAudited: 2026-03-24
---

## Purpose

A working staging folder containing all 118 orchestrator tab .mdx files copied from `v2/orchestrators/` for analysis, rewriting, and content production work. Also contains three strategic planning documents (01, 02, 03) defining copy standards, a page-by-page critical analysis, and three options for autonomous content production. No files in `v2/orchestrators/` have been modified — all edits happen here only.

## Verified Done

**Strategic planning documents — all three contain real content:**
- `01-CORE-NEEDS-AND-STANDARDS.md` — complete copywriting standards document: arriving question, primary persona, voice register, page structure rules, component conventions, quality gates, terminology lock (15 terms), The 10% Rule
- `02-CRITICAL-ANALYSIS.md` — complete critical analysis of all 118 pages: P0–P3 issues per page, voice scores, gap analysis. 14 P0 blockers identified, 28 P1 issues, 35+ P2 issues. Sections: Start Here, Concepts, Quickstart, Setup, Guides, Resources
- `03-AUTONOMOUS-PRODUCTION-OPTIONS.md` — three detailed production options (Assembly Line, Parallel Tracks, Scorecard Loop) with prompt templates, effort estimates, and a hybrid recommendation

**Copied page files — verified by spot-check:**
- Published pages (e.g., `setup/configure.mdx`, `guides/ai-and-job-workloads/diffusion-pipeline-setup.mdx`) contain full production content with real MDX structure, imports, and components
- Draft pages (e.g., `guides/roadmap-and-funding/funding-and-support.mdx`) have frontmatter and placeholder comments but no substantive body content
- Stub pages (e.g., `resources/technical/x-contract-addresses.mdx`, `resources/community-pools.mdx`) exist as confirmed stubs — frontmatter only, no content

**Folder structure mirrors `v2/orchestrators/` canonical layout** across all sections: concepts, quickstart, setup, guides (7 sub-sections), resources (technical, compendium).

## Claimed Done — Unverified or Incomplete

**"118 files copied"**: The Appendix of `03-AUTONOMOUS-PRODUCTION-OPTIONS.md` states 118 files copied. `02-CRITICAL-ANALYSIS.md` states "73 published + 45 _workspace/composable" = 118 total. However, MASTER-STATE.md in ORCHESTRATOR-CONTENT-WRITING describes 84 active .mdx files and 72 in docs.json nav. The count discrepancy (118 vs 84 vs 72) is unexplained. The ORCHS folder appears to include `_workspace/` composable page fragments that are not navigation-facing pages, which inflates the count. No single file reconciles these counts.

**Composable pages folder** (`guides/tutorials/gateway-tutorial-composable-pages/stubs/`): Contains stub files (`tutorial-byoc-cpu-pipeline.mdx`, `tutorial-go-production.mdx`, `tutorial-offchain-transcoding-test.mdx`) — these appear to be page composition fragments, not standalone pages.

**02-CRITICAL-ANALYSIS.md P0 items**: 14 P0 blockers are identified and documented but no fixes have been applied to any ORCHS page file. The analysis is a findings report; no production work has executed against it yet.

## In Progress

No active work visible in this folder. The strategic documents describe three production options but no option has been formally selected and no content rewrites have begun. The ORCHS folder is in a "ready for execution, awaiting decision on production approach" state.

The 03-AUTONOMOUS-PRODUCTION-OPTIONS.md recommends a hybrid of Options B+C with a 3-week execution timeline. This has not been actioned.

## Not Started / Still To Do

**Production approach selection**: Alison has not selected Option A, B, C, or the hybrid. No work has begun on any of the 4 or 5 tracks described.

**P0 blocker resolution**: 14 P0 items across the tab remain unaddressed:
- 4 stub pages with no content (funding-and-support, orchestrator-profiles, community-pools, x-contract-addresses)
- 18 pages with unresolved TODO/REVIEW comments
- Specific items: AIServiceRegistry status, dual-mode VRAM verification, reward call worked example verification, dl_checkpoints.sh verification

**Numbers pass**: 60%+ of pages lack concrete earnings, cost, or threshold data (per 02-CRITICAL-ANALYSIS.md). No numbers pass has run.

**Banned-word sweep**: Not run on any page.

**Voice rewrites**: Not started. ~15 pages score below 8/10 on voice compliance.

**Cross-reference audit**: Not done.

**Naming audit**: Not done.

**Deployment to v2/orchestrators/**: No changes have been made to any live file. All ORCHS work remains in the working folder only.

## Blockers

**Production approach decision**: Which of the three options (or the hybrid) to use. This gates all execution work.

**P0 human verification items**: Several P0 items cannot be resolved autonomously:
- AIServiceRegistry integration status (requires protocol team input)
- Dual-mode VRAM budgets (requires benchmarking)
- connect-and-activate.mdx — 11 unresolved TODOs (requires SME verification)
- Reward call worked example and profitability calculation (requires protocol repo verification)

**ORCHS vs CONTENTI-PIPLEINE relationship**: The ORCHS folder contains copies of the v2/orchestrators/ live files for improvement. The CONTENTI-PIPLEINE/content/ folder contains 12 newly written S01-S12 section pages that are not derived from the same source set. These two tracks are producing different outputs for the same tab. Which track's output becomes canonical for `v2/orchestrators/` has not been decided.

**ORCHS folder is not the canonical output destination**: `v2/orchestrators/` receives canonical changes. ORCHS files must be reviewed, reconciled, and migrated — the migration path is undefined.

## File Inventory Flags

**Deprecated files in ORCHS**: Several pages have `x-deprecated/` or `dep-` prefix files copied alongside current versions:
- `setup/x-deprecated/` — 4 files (dep-activate, dep-config, dep-s-guide, r-configure)
- `guides/deployment-details/x-deprecated/` — 9 files
- `guides/operator-considerations/x-deprecated/` — 3 files
- `concepts/x-deprecated/` — 5 files
These are copies of the deprecated source files. They should not receive content work — they exist for reference only.

**Two pool guides present**: `guides/deployment-details/join-a-pool.mdx` (older) and `guides/deployment-details/new-join-a-pool.mdx` (newer) both copied. The reconsolidation plan in CONTENTI-PIPLEINE (04.5) calls for dropping the old one, but this has not been executed.

**community-pools.mdx**: Appears in ORCHS as `status: review` with an empty body — stub page marked as under review, which is misleading.

**`x-contract-addresses.mdx`**: Has a typo in the title ("Adresses") and `audience: developer` — wrong audience for the orchestrators tab.

**No tracking file**: ORCHS has no internal tracker or status board. Progress against the 3-option production plan cannot be tracked without one.

## Audit Notes

ORCHS is a well-conceived staging folder with excellent analytical groundwork. The three planning documents (01-03) are thorough and accurate. However, this folder is entirely pre-execution: no content has been improved, no P0 blockers have been resolved, and no production approach has been selected. The gap between the quality of the analysis and the absence of any execution is the defining characteristic of this plan's current state.

The relationship between ORCHS (working copies of current v2/orchestrators/ pages) and CONTENTI-PIPLEINE (freshly written S01-S12 pages) is unresolved. Both are producing content for the orchestrators tab through different approaches. A decision on which work stream is canonical for `v2/orchestrators/` is required before either can be merged.

The 02-CRITICAL-ANALYSIS.md is the most immediately useful file in this folder — it gives a complete, page-by-page diagnosis of all publication blockers. Whoever executes the production work should treat it as the primary work order.
