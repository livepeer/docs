---
title: Documentation Governance — Research (Plan Review + Docs-Guide Scan)
status: active
owner: DOCUMENTATION
created: 2026-03-21
---

# Research: Active Plan Review + Docs-Guide Scan

This file summarises findings from reviewing all active workspace plans and scanning the `docs-guide/` folder. These findings feed directly into `audit.md`.

---

## 1. Active Plans Summary

### REPO-STRUCTURE-GOV
**Status**: Active. ~60% executed.
**Owns**: Root folder layout, `docs-guide/` restructure, `v2/` folder violations, `snippets/assets/` cleanup.
**Key decisions confirmed**:
- `docs-guide/` structure is now locked: 6 canonical sections + `_workspace/`
- Naming conventions confirmed: `-catalog.mdx`, no `-index.mdx`, no 0-byte stubs
- 5 docs-guide template types created in `snippets/templates/docs-guide/`
- CI pipelines added for catalog freshness enforcement
- `docs-guide/policies/docs-guide-structure-policy.mdx` written (authoritative)

**Pending (relevant to docs):**
- Phase 1.3: `contribute/` → `docs-guide/contributing/` — NOT YET DONE. Two contributor guide locations active simultaneously.
- Phase 2C: 21 v2/ folder violations (loose `.md`, `_contextData_`, `x-archived/` etc.) — pending human approval.
- Phase 2E.3: `tools/vscode/components/component-registry.json` vs `docs-guide/component-registry.json` — sync unconfirmed.
- Phase 3: `_workspace/` standardisation across root folders — not started.
- Phase 4: Governance docs update (AGENTS.md, root-allowlist) — pending Task 14 + 2C.

**Documentation implications**: `docs-guide/` structure is stable and governed. But its public surface (`v2/resources/documentation-guide/`) is not yet synced with the `docs-guide/` restructure.

---

### SCRIPT-GOVERNANCE
**Status**: Active. Most taxonomy tasks done. Task 14 (migration to `operations/`) pending merge.
**Branch**: `docs-v2-dev-scripts`
**Three-tier taxonomy** (APPROVED):
```
<type> / <concern> / <concern-niche>
Types: audits/, generators/, validators/, remediators/, dispatch/, automations/
Concerns: content/, components/, governance/, ai/
```
**Total scripts**: 179 (158 in tools/scripts/, 4 in .githooks/, 4 in .github/scripts/, 8 in snippets/automations/)

**Key documentation issues found**:
- `docs-guide/policies/script-governance.mdx` exists as a hand-maintained policy
- `workspace/plan/active/SCRIPT-GOVERNANCE/script-docs.md` + `script-framework.md` exist as planning artifacts
- `docs-guide/catalog/scripts-catalog.mdx` is the generated output
- `structure.md` file referenced in plan.md does NOT exist in the folder (missing)
- After Task 14 lands, all catalog generator script paths will need updating (they reference `tools/scripts/`)
- `docs-guide/catalog/components-catalog.mdx` generator banner path is STALE (flat path vs. three-tier)

**Documentation implications**: Three-tier taxonomy should inform how documentation about scripts is structured. The `<type>/<concern>` model is the right template for all repo inventory documentation.

---

### COMPONENT-GOVERNANCE
**Status**: Nearly complete. Merged to `docs-v2-dev` commit `be1e2d55`. T16 (tests), T20, T21 pending.
**Target structure**: `snippets/components/` with 5 top-level categories: `elements/`, `wrappers/`, `displays/`, `scaffolding/`, `integrators/` — each with sub-niches.
**Registry**: `docs-guide/component-registry.json` is canonical for component inventory.
**Usage map**: `docs-guide/component-usage-map.json` generated by `scan-component-imports.js`.

**Key documentation issues found**:
- `docs-guide/catalog/components-catalog.mdx` shows category = "undefined" — generator not re-run after restructure
- Generator banner in `components-catalog.mdx` shows stale path `tools/scripts/generate-docs-guide-components-index.js` (actual: `tools/scripts/generators/governance/catalogs/generate-docs-guide-components-index.js`)
- Component docs exist in THREE places:
  1. `docs-guide/frameworks/component-governance.mdx` — governance framework
  2. `docs-guide/catalog/components-catalog.mdx` — generated inventory
  3. `v2/resources/documentation-guide/component-library/` — public human guide (7 pages)
- `docs-guide/docs-glossary.md` still references `tasks/plan/active/COMPONENT-GOVERNANCE/structure.md` (stale path)
- `handoff-component-usage-map.md` was delivered to this plan 2026-03-21

---

### CONTENT-WRITING
**Status**: Active — in Phase 1 definition stage. Pilot: `gateways/guides`.
**Owns**: `v2/` page content framework — page types, templates, voice, layout.
**Plans**: 3-phase pipeline (Define → Build → Run). All of Phase 1 is pending.
**Relevant research files**:
- `workspace/plan/active/CONTENT-WRITING/Prompts/` — 5 new prompt files (tab-map, pass-a, pass-b, section-naming, voice-rules)
- `workspace/plan/active/CONTENT-WRITING/Research/` — research folder

**Documentation implications**: `docs-guide/frameworks/page-taxonomy-framework.mdx` and `docs-guide/frameworks/content-system.mdx` will need updates once Phase 1 definitions are agreed. Currently 12 pageTypes defined but under review.

---

### AI-TOOLS-GOVERNANCE
**Status**: Active. `client-side-component-audit.md`, `plan2.md`, `structure.md` present.
**Owns**: `ai-tools/` placement, AI adapter files, AI discoverability.
**`ai-tools/` structure**:
```
ai-tools/
├── agent-packs/         (claude/, codex/, cursor/, skills/, windsurf/)
├── ai-rules/            (protocol files, safety rules, override policy)
├── ai-skills/           (content-pipeline-pass-a, pass-b, tab-map, + many more skill types)
├── registry/            (ai-tools-registry.json, ai-tools-inventory.md, schema, README)
└── _workspace/
```
**Key documentation issues**:
- `docs-guide/tooling/ai-tools.mdx` documents the AI tools system (hand-maintained)
- `docs-guide/features/ai-features.mdx` is a near-empty STUB (20 lines of placeholder) despite being a "required" file
- `v2/resources/documentation-guide/ai-features.mdx` exists as a separate public page
- AI adapter files (`.claude/CLAUDE.md`, `.cursor/rules/`, `.windsurf/`, `.augment/`, `.github/copilot-instructions.md`) all govern agent behavior but are each maintained separately
- The `ai-tools-registry.json` is the canonical source for AI tools inventory — but its documentation coverage vs the skills catalog split is unclear to contributors
- Recent AI discoverability work (commits ~2026-03-18) added companion schema/manifest files — their documentation is not yet reflected in docs-guide

---

### CANONICAL-TRUTH-GUIDES
**Status**: Stub. Only `notes.md` exists.
**Content**: "need to update all docs and scripts with the combined new frameworks for content/frontmatter, scripts & components; also need to do a repo framework & oss governance one."
**Implication**: This plan acknowledges the problem but has no execution structure. The DOCUMENTATION plan (this folder) should absorb or coordinate with it.

---

### OSS-OWNERLESS-REPO-GOVERNANCE
**Status**: Active. Foundation implemented.
**Owns**: Ownerless governance rollout — each surface must have canonical source + deterministic validator + exact repair path + primary gate.
**Canonical policy doc**: `docs-guide/policies/ownerless-governance.mdx`
**Machine-readable manifest**: `tools/config/ownerless-governance-surfaces.json`
**Implemented surfaces**: portable skills, docs-guide catalogs, UI templates, GitHub/contributor governance, script governance (migrating), component governance (migrating).
**Documentation implication**: Every new documentation surface added by this DOCUMENTATION plan should conform to the ownerless model — i.e., have a validator and repair path, not just a policy doc.

---

### TOOLING (lpd CLI audit)
**Status**: Active. Audit complete, fixes deferred to after Task 14.
**Finding**: 12 stale script paths in `lpd` CLI — affects `lpd ci`, `lpd repair`, `lpd setup`.
**Documentation implication**: `docs-guide/tooling/lpd-cli.mdx` is noted as "not auto-updated" — needs a freshness gate.

---

### CONTENT-STRUCTURE-TEMPLATES
**Status**: Early research. `pre-research.md`, `research.md`, `resources.md` only.
**Scope**: Page templates, pageType framework.
**Implication**: `snippets/templates/pages/` and `snippets/templates/blocks/` are the canonical template sources; `docs-guide/catalog/templates-catalog.mdx` is the generated index. The gap is the pageType → template mapping (CONTENT-WRITING Phase 1 is defining this).

---

### TERMINOLOGY-COLLATE
**Status**: Active — has consolidated/ and per-tab/ glossary files.
**Documentation implication**: There are 3+ glossary/terminology locations (see audit.md Section 5). The TERMINOLOGY-COLLATE plan is collating terms per tab but doesn't resolve the source-of-truth split between `docs-guide/docs-glossary.md` (internal contributor terms) and `v2/resources/livepeer-glossary.mdx` (public).

---

## 2. Docs-Guide Scan Summary

### Folder structure (current, confirmed)
```
docs-guide/
├── catalog/           6 files — all generated (scripts, workflows, templates, pages, components, ui-templates)
├── contributing/      2 files — contributing.mdx, mintlify.mdx
├── features/          7 files — feature-map, architecture-map, automations, data-integrations, ui-system, visual-explainer-workflows, ai-features
├── frameworks/        4 files — component-governance, content-system, page-taxonomy-framework, research-skill-workflow
├── policies/          15 files — source-of-truth-policy, generated-artifact, v2-folder-governance, quality-gates, audit-system-overview, skill-pipeline-map, cleanup-quarantine-policy, component-layout-decisions, infrastructure-principles, ownerless-governance, root-allowlist-governance, agent-governance-framework, docs-guide-structure-policy, workspace-lifecycle-policy, script-governance, snippets-assets-policy
├── tooling/           5 files — ai-tools, dev-tools, lpd-cli, lpd-mdx-preview, reference-maps/icon-map
├── _workspace/        archive/, drafts/, index.md
├── source-of-truth-guide.mdx   (root-level landing page)
├── docs-glossary.md             (internal contributor terminology)
├── component-registry.json      (generated — canonical component inventory)
├── component-registry-schema.json
├── component-usage-map.json     (generated)
└── [legacy files in _workspace/02_Design-Specification/ and 03_Component-Governance-Framework/]
```

### Key issues noted during scan
1. `docs-guide/features/ai-features.mdx` is a nearly-empty stub — listed as required in source-of-truth-policy but essentially empty
2. `docs-guide/docs-glossary.md` has stale `tasks/` path reference (should be `workspace/`)
3. `docs-guide/contributing/` is partial — `contribute/CONTRIBUTING/` root folder hasn't moved yet
4. `_workspace/` has legacy `02_Design-Specification/` and `03_Component-Governance-Framework/` zip trees — should have explicit 90-day TTL noted
5. No docs in `docs-guide/` cover the `.env.example` / secrets documentation model
6. No machine-readable index connecting docs-guide pages to their audience (human vs. agent) or maintenance mode (generated vs. hand-maintained) — this distinction only exists as prose in the structure policy

---

## 3. Public Documentation Surface Scan

### `v2/resources/documentation-guide/` (public)
11 pages + component-library/ subfolder (7 pages = 18 total):
- `documentation-guide.mdx`, `documentation-overview.mdx` — overview/guide pages
- `authoring-standard.mdx`, `style-guide.mdx`, `research-and-fact-checking.mdx` — authoring guidance
- `ai-features.mdx`, `docs-features-and-ai-integrations.mdx` — AI tooling
- `automations-workflows.mdx`, `snippets-inventory.mdx` — tooling
- `contribute-to-the-docs.mdx` — contributor entry
- `component-library/` — 7 pages (one per component category)

### `v2/internal/` (internal team)
22 pages including:
- `overview/` subfolder — governance, personas, about, docs-philosophy, governance-pipeline, strategic-alignment
- `rfp/` — deliverables, outcomes, report, problem-statements, aims
- `assets/transcripts/` — a16z, ycomb transcripts
- Loose: `definitions.mdx`, `references.mdx`, `docs-philosophy.mdx` (DUPLICATE of overview/docs-philosophy.mdx), `ecosystem.mdx`

---

## 4. Secrets / Env Documentation Scan

**Current state**:
- `.env.example` at repo root: well-maintained, covers ALL secrets (YouTube, Ghost, Discord, GitHub Token, DOCS_V2 PAT, OpenRouter, Speakeasy, Notion, Showcase pipeline). Last updated 2026-03-21.
- `tools/notion/.env.example`: Notion-specific credentials only.
- `tools/config/.env.docsearch`: Algolia/docsearch config (not documented as secrets).

**Gaps**:
- No link FROM docs-guide → `.env.example`. The secrets reference is discoverable only if you know to look at the root of the repo.
- No policy on whether `.env.example` should be kept in sync with CI/GitHub Secrets — no validator exists.
- No ownerless surface entry for `.env.example` in `ownerless-governance-surfaces.json`.
- `docs-guide/features/data-integrations.mdx` likely covers data pipeline integrations — but may not cross-reference secret requirements per pipeline.

---

## 5. Ownerless Governance Scan

**What exists**:
- `docs-guide/policies/ownerless-governance.mdx` — policy doc
- `tools/config/ownerless-governance-surfaces.json` — machine-readable surface manifest
- `tests/unit/ownerless-governance.test.js` — validator
- `lpd repair --surface <id>` — repair CLI

**Surfaces tracked** (from OSS plan): portable skills, docs-guide catalogs, UI templates, GitHub governance, script governance, component governance, frontmatter contract.

**Not tracked as ownerless surfaces**:
- `.env.example` / secrets documentation
- `docs-guide/` hand-maintained canonical files (no freshness validator beyond catalog files)
- AI adapter file parity (`.claude/`, `.cursor/`, `.windsurf/`, `.augment/` — no sync validator)
- `v2/resources/documentation-guide/` public pages vs `docs-guide/` canonical pages

---

## Summary Table: What Each Plan Owns for Documentation

| Plan | Documentation surfaces owned | Current doc gap |
|---|---|---|
| REPO-STRUCTURE-GOV | `docs-guide/` structure; `contribute/` move | `contribute/` still not moved; v2/ violations pending |
| SCRIPT-GOVERNANCE | `scripts-catalog.mdx` generation; script header schema | Generator paths stale post-Task 14; `structure.md` missing |
| COMPONENT-GOVERNANCE | `components-catalog.mdx`; component-registry.json; component-usage-map.json | Generator not re-run; stale banner; public component library pages not updated |
| CONTENT-WRITING | Page taxonomy; pageType docs; content framework | Still in definition phase; page-taxonomy-framework.mdx may be outdated |
| AI-TOOLS-GOVERNANCE | `ai-tools.mdx`; AI discoverability; adapter files | `ai-features.mdx` is a stub; companion docs not in docs-guide |
| OSS-OWNERLESS | Ownerless surface manifest; repair paths | Secrets/.env not an ownerless surface; hand-maintained docs have no validator |
| CANONICAL-TRUTH-GUIDES | "All docs with combined frameworks" — undefined | No plan, no execution |
| DOCUMENTATION (this) | Holistic documentation governance | New — this plan |
